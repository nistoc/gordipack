# Разбор свода правил: что годится любой команде, что — типу команд, что — только нам

**Слово владельца 2026-08-08 11:02 UTC:** «разбери 42 правила на общие и наши».
**Повод (замер 10:48):** в шаблоне универсальных правил четыре, у нас нажито своё — всё
ценное живёт только в нашей базе, новая команда получит почти пустой свод.

## Счёт, чтобы никто не потерялся

```
записей в своде ........................ 45
надгробий (отозваны, не предписывают) ...  7   — не разбираются: мёртвое не раздают
✅ живых ................................ 38
   уже в шаблоне ......................... 7   (4 универсальных + 3 предметных)
   разобрано ниже ....................... 31
   ────────────────────────────────────────
   универсальных (в шаблон) ............. 21
   предметных (в пресет data-platform) ... 3
   наших (остаются только у нас) ......... 7
   21 + 3 + 7 = 31 ✓
```

⚠️ Владелец назвал «42» — это 45 минус 3; на деле сеяных из шаблона 7, а живых 38.
Число уточнено замером, предмет разбора тот же.

## Уже в шаблоне — трогать не надо

> ⚰️ ПОПРАВКА 15.08 (замером, счёт по ключам файлов): раздел ниже и раздел «Что дальше»
> ОТСТАЛИ ОТ СОБСТВЕННОГО ИСПОЛНЕНИЯ. Блок v3 в universal.sql (перенос 08.08 по слову
> «переноси правила в шаблон») уже содержал ВСЕ 21 универсальных из таблицы этого файла;
> на 15.08 недоставало только трёх предметных — перенесены блоком v2 в data-platform.sql
> (слово владельца на состав — 14.08). Итог: universal.sql — 35 ключей · data-platform.sql — 6.
> Этот же файл учил «в шаблоне 7» и «шаблон НЕ правлен» — классический разбор, отставший
> от переноса, сделанного в тот же день.

| правило | где лежит |
|---|---|
| one-writer-one-channel · append-only-messages · no-push-without-owner · rule8-destructive | rules/universal.sql |
| no-scan-external-contours · migration-safety · semantic-canon | rules/domain-specific/data-platform.sql |

## УНИВЕРСАЛЬНЫЕ — годятся любой команде из шаблона (21)

Признак класса: правило описывает МЕХАНИЗМ самого шаблона (лента, слепки, задачи, приёмка)
или норму со-работы, не привязанную к предмету. Что вычистить при переносе — в третьей графе.

| правило | почему универсально | что обезличить |
|---|---|---|
| task-discipline | задача не живёт в чате; критерий приёмки обязателен. Механизм backlog — часть шаблона | историю решений (даты, №№ записок) — остаётся у нас |
| full-scan-every-tick | контракт чтения ленты с разрезанным токеном — механизм read-messages шаблона | прецедент STUD 12.07 |
| note-format | шапка ноты, суть первой строкой, UTC | — |
| note-priority-norm | когда нота срочная; введена замером | сам замер (№ записки) |
| poll-format | heartbeat через write-message --poll — механизм шаблона | № записок |
| ack-deadline | «взял/не возьму», контракт на тишину | эталон 16.07, №№ |
| escalation-and-ground-truth | молчание при открытом хендшейке → пинг → эскалация | №№ записок |
| milestone-single-source | веха один раз нотой, остальное — ссылка | — |
| shared-broadcast-channel | broadcast тегом ALL — скрипт в шаблоне | — |
| phoenix-save-on-stop | слепок при STOP — сердце механизма перерождения | — |
| rhythm-survives-rebirth | ритм не переживает пробуждение: таймер первым ходом | историю ING 23.07 |
| dowry-facts-carry-source | факт в слепке новой роли несёт источник и дату | № записки-основания |
| timestamp-utc-in-sqlite | UTC везде с явным суффиксом | цитату слова владельца |
| gate-before-commit | зелёный build+test перед коммитом | предупреждение про наш гейт |
| contract-diff-before-commit | дифф контракта до кода, OK потребителя | — |
| acceptance-e2e | приёмка владельцем на КОНКРЕТНЫХ примерах, по артефактам, не рапортам | путь к тест-плану atlas.studio, эталон 16.07 |
| remeasure-before-ask | перед вопросом владельцу перемерить замер вплотную | нашу историю двух откатов |
| proactive-next-steps | простой без задач → предложить 1–2 шага | — |
| questions-via-interface | вопросы владельцу структурно, варианты + trade-off | — |
| revision-bump-same-stroke | версия артефакта бампается тем же ходом, что правка | №№ |
| coord-commits-coordination | координационный репозиторий коммитит только координатор, add поимённо | имя репо atlas.archs, наши зоны |

## ПРЕДМЕТНЫЕ — в пресет data-platform (3)

Признак: про данные/миграции/живую БД — нужны команде такого типа, бессмысленны для чисто
фронтендной.

| правило | почему предметное | что обезличить |
|---|---|---|
| data-migration-provenance | мутация данных мимо API обязана оставить след-запись | phd1, PROV-O-детали наши |
| migrations-core-only | у схемы БД один владелец-роль; чужая зона — по контракт-диффу | имена CORE/ING, atlas_ingest |
| migrations-under-watch | hot-reload сервис накатывает миграции САМИМ сохранением файла — класс тихой мутации | dotnet watch/пути — оставить как пример |

## НАШИ — остаются только в нашей базе (7)

Признак: имена наших ролей/репо/путей составляют СУТЬ правила, а не пример в нём.

| правило | что держит его у нас |
|---|---|
| core-standing-permissions | стоячее разрешение КОНКРЕТНОЙ роли на КОНКРЕТНЫЙ репозиторий |
| ing-standing-permissions | то же |
| role-roster-and-zones | реестр состава ИМЕННО нашего контура (11,5 КБ имён и зон) |
| file-map | карта наших путей. Принцип «у контура есть файл-карта» можно вынести строкой в README шаблона — само правило не переносимо |
| md-to-sqlite-phased-cutover | история НАШЕГО перехода в БД; у команды из шаблона БД с рождения — правило ей не о чем |
| role-migration-standard | признаки перехода на НАШ новый стандарт; шаблон уже включает стандарт из коробки |
| services-self-raise | наши сервисы, skill atlas-local, наши PID-пути. Принцип «свой сервис поднимаешь сам, чужой не рестартуешь» — кандидат в предметные, если будет второй потребитель |

## Что дальше

⛔ Шаблон НЕ правлен: состав — слово владельца. После слова:
1. 21 текст переписывается обезличенно в rules/universal.sql (у нас остаются полные,
   с историей и основаниями — история команды принадлежит команде);
2. 3 — в data-platform.sql;
3. новые четыре поля (basis/authorized/source_ref/expiry_kind) заполняются у переносимых
   сразу — они рождаются в шаблоне заново, отказ инструмента их и так потребует.
