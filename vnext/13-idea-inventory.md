# Что меняем в механизме со-работы — план и состояние

_Собран 2026-08-06 12:47 UTC по запросу владельца: «расскажи понятными словами, что есть,
чтобы я мог дополнить и расставить порядок»._
_Переписан 2026-08-07 14:00 UTC: убраны промежуточные отчёты и внутренний жаргон._

> ⚖️ **Числа — на 2026-08-07 13:55 UTC, взяты запросом к живой базе, а не по памяти.**
> База меняется каждые минуты. **Разошлось — верна база, а не документ.**
> Прежние редакции целиком лежат в истории репозитория.

---

## 📖 СЛОВАРЬ — чтобы дальше не спотыкаться

| Говорю так | Значит |
|---|---|
| **проверка** | программа, которая испытывает механизм и краснеет, если он неверен |
| **проверка с нарочной поломкой** | то же, но про неё ДОКАЗАНО, что она кусается: механизм ломают специально и смотрят, заметит ли. Не заметившая поломки — ничего не доказывает |
| **встречный случай** | похожая, но ЗДОРОВАЯ ситуация рядом. На ней проверка обязана молчать — иначе кричит всегда, и её перестают читать |
| **постоянная проверка** | смотрит не на механизм, а на живое состояние работы: не нарушил ли кто договорённость прямо сейчас |
| **память роли** | что роль читает при пробуждении в новом чате, чтобы продолжить работу |
| **отметка «докуда дочитано»** | место в общей ленте, до которого роль дошла |
| **свод правил** | правила контура; живут в базе данных |
| **файл с правилами** | человекочитаемая копия свода, собирается из базы программой |
| **записка** · **карточка** | сообщение в общей ленте · задача в общем списке задач |

---

# ЧТО ДЕЛАЕМ ДАЛЬШЕ — состояние на 2026-08-09 13:36 UTC
> Числа ниже **перемерены по живой базе и по коду прямо сейчас**, а не переписаны из прежней
> редакции этого файла. Дважды за неделю статус в нём протухал на сутки и выглядел достоверным.

### 🐤 ПРОБА-КАНАРЕЙКА СОСТОЯЛАСЬ 09.08 — ГЛАВНОЕ СОБЫТИЕ С ПРОШЛОЙ РЕДАКЦИИ
Владелец решил раскатывать по одной роли и начал с меня (слово 08.08 23:22 UTC:
«сначала одна роль как проба»). Я перезапустился в новом чате 09.08 и **проверял три
заранее объявленные вещи** — то есть проверял не работу, а сам механизм:
```
① не начал ли новый чат с ОТКАЗОВ от того, что уже разрешено ...... ✅ отказов НЕ БЫЛО
② показал ли механизм САМ, без напоминания: долг ленты · кто звал
   лично · открытые карточки · что менялось в своде ................ ✅ показал
③ узнал ли я свои права ЗАПРОСОМ к базе, а не воспоминанием ........ 🔴 КОМАНДА ПАДАЛА
```
🎯 **Третий пункт и есть главный результат — лучший, чем три зелёных.** Команда «что мне
разрешено» из промпта запуска **падала у ЛЮБОЙ роли**: имя роли готовилось и терялось
по дороге в запрос. Без имени роли она работала — поэтому никто и не заметил.
> **Зелёная проверка означает ровно то, что она позвала. Не больше.**

И вторая находка, тише и опаснее: стоячее разрешение отправлять код записано на ВСЕХ,
а отбор искал строки с именем конкретной роли ⇒ роль, честно спросив «что мне разрешено»,
услышала бы **пусто при живом разрешении владельца**. **Падение видно. «Пусто» читается
как ответ.**
✅ Оба починены, приёмка выросла с 10 случаев до 14 и теперь зовёт форму С ИМЕНЕМ РОЛИ;
каждая из трёх починок проверена **нарочной поломкой** — возвратом назад по очереди.

### 🎯 ЧЕГО ЖДЁМ ОТ ВЛАДЕЛЬЦА — ОСТАЛОСЬ ДВА, И НИ ОДНО НЕ ПРО СВОД ПРАВИЛ
```
✅ отправка ..... запрет СНЯТ ЦЕЛИКОМ 08.08 15:56 UTC («пушить можно»); разрушающее осталось
✅ правила ...... основания закрыты ЦЕЛИКОМ: 0 из 40 живых без основания (было 13 у координатора)
✅ проба ........ решена и ПРОВЕДЕНА: первой шёл PROTO, следующая — CORE (ваше слово 09.08 10:26)
👉 осталось:
   ① имя правила «читать всё каждый ТАКТ» — такта нет. Переименовать (ломает ссылки
      в других правилах и в памяти ролей) или оставить, приписав, что имя историческое
   ② снимки живой страницы Перископа — карточка #105 висит с 07.08, без них не закрыть
```
📌 **ГРАНИЦУ НАБОРА НАЗЫВАЮ НАМЕРЕННО, потому что вчера на ней и споткнулся.** Вчера здесь
стояло «у ВАС без основания ноль» — верно про ваши 23 правила и ложно про свод целиком:
у координатора тогда 13 из 17 жили без полей. Он их закрыл сам, и теперь ноль — про ВЕСЬ
свод, 40 живых правил. Урок остаётся:
> **число верно, а фраза вокруг числа шире числа.**
### ✅ ОБЕ РАЗВИЛКИ РЕШЕНЫ 08.08 И ИСПОЛНЕНЫ
```
① СРОЧНОСТЬ — выбран ход Г (вычислять, не хранить). ✅ ВРЕЗАНО координатором.
   Порог 12 ч, причина снятия печатается для КАЖДОЙ записки («старше порога» /
   «отвечено там-то»). Проверка моя, 8 случаев на ЖИВОМ модуле — зелёная.
   🎯 Главное из замера, а не из спора: пометка не ускоряла ответ НИКОГДА
      (0,2 ч и на срочное, и на обычное) — чинили не работающее, а неработавшее
② ОСНОВАНИЕ У ПРАВИЛ — выбран вариант с полями + ОТКАЗОМ при сохранении.
   ✅ ВРЕЗАНО: пять полей, отказ учит, а не просто не пускает.
   ➕ Владелец добавил ЧЕТВЁРТОЕ поле, которого не было в развилке: УСЛОВИЕ ОТМЕНЫ.
      Довод подтвердился нашими же надгробиями: шесть правил умерли, когда их условие
      наступило ЗАРАНЕЕ, и не заметил никто — до ручного пересмотра свода
   ⚰️ ЗДЕСЬ СТОЯЛО «сосуд пуст: 0 из 45, отказ ни разу не сработал на живом» — верно
      на 08.08 14:40 и НЕВЕРНО с вечера того же дня. Снимаю надгробием, а не молча:
      строка спорила с числами в собственном документе двумя экранами ниже
   ✅ ЗАМЕР 09.08 13:36 UTC: основание заполнено у 30 правил, БЕЗ ОСНОВАНИЯ — 0 из 40 живых.
      Отказ сработал на живом в тот же вечер (код возврата 2, в базу не попало)
```

### ✅ ЗАКРЫТО ЗА СМЕНУ 08.08 — ЧЕТЫРЕ КРУПНЫХ ХОДА
```
ПАМЯТЬ РОЛИ (2.5) ....... машинный слой НЕ ХРАНИТСЯ: собирается при каждом чтении слепка.
   Долг ленты числом · кто звал ЛИЧНО · новее ли своя записка слепка · что правлено в своде.
   ✅ врезано в живой read-phoenix, приёмка 8 случаев, ПРОЧИТАНО ЧУЖОЙ РОЛЬЮ и принято ею
АДРЕСАТ ПОЛЕМ (1.1) ..... таблица имён + ручки --to/--cc + витрина «только моё».
   Было: ручка ПРИНИМАЛА имя и ВЫБРАСЫВАЛА его. Стало: 5658 адресатов на 1015 записок.
   📏 роли теперь могут не читать от 67 % до 98 % своего долга: CHROME 329→38 · RCC 431→16
   ⚖️ и потолок печатается при каждом прогоне: половина ленты адресована прозой без «собаки» —
      витрина её не покажет, и её молчание значит «нечем отбирать», а не «вам не писали»
ПРАВИЛА В ШАБЛОН ........ 9 новых + 6 углублённых; сборка СВЕЖЕГО контура вскрыла
   противоречие внутри свода (местное время против UTC) — снято
ПРОВЕРКА ВЫЗОВОВ ........ «правило верно ≠ правило позвано верно»: обязательный довод,
   потерянный в вызове. Заказан координатором, 7 случаев, 6 различающих
```
📌 Общий прогон в 14:35 впервые за двое суток показал **все 18 постоянных проверок зелёными.**
🪤 **И тут же поправка против себя: в 14:46 это уже неправда — один снова красный**
(«пишут, но не читают: 1 из 3 активных») и три жёлтых, включая мой собственный слепок,
отставший от моей же последней записки на 19,7 часа.
⇒ Оставляю обе строки рядом нарочно. **«Зелено» — это не свойство контура, а показание
прибора в момент замера.** Написать «всё зелено» без времени значило бы выдать снимок
за состояние — ровно тот класс, из-за которого этот файл дважды протухал на сутки.

### ✅ ЗАКРЫТО ЗА СМЕНУ 07.08 (каждое принято ПРОГОНОМ, не словом)
```
свод правил ....... `task-discipline` v3 внесено моей рукой по живому слову владельца:
                    критерий приёмки ОБЯЗАТЕЛЕН · описание непустое · ссылка разрешима
пересбор зеркала .. перенесён в ШАБЛОН gordipack: set-rule сам зовёт генератор,
                    проверка «файл против базы» в общем прогоне, приёмка на СВЕЖЕМ контуре
пометка «перенесено» ✅ внесена координатором 10:28, мой укус 3 из 3 зелёных (карточка #88)
поле «показано» ... ✅ внесено, укус переписан на верное свойство, 4 свойства держатся
защита памяти роли  ⛔ «OK» на пустой слепок больше не проходит: отказ, прежний текст цел,
                    печатается «было N → стало M». Случай @RCC — секция обнулилась молча
проверка памяти роли ⑥ больше не гасится пересохранением вслепую: пока механизм правится,
                    случай откладывается — и отсрочка ПЕЧАТАЕТ СВОЙ РАЗМЕР, а не молчит
дрейф копий ....... 6 файлов разошлись В ОБЕ СТОРОНЫ; разобраны пофайльно по ИМЕНАМ
                    (что пропадёт), сведены, потеряно ноль
одна команда на все приёмки  `bite-all.py`: ТРИ исхода — держится / сломано / НЕ ЗАПУСТИЛАСЬ
```

### 🔬 НОВЫЕ ПРОВЕРКИ ЗА СМЕНУ — у каждой своя приёмка на нарочной поломке
```
guard-printed-names ....... механизм печатает имя, которого у него нет (3 живые находки)
check-dangling-refs ....... ссылка, неразрешимая вне породившего чата (уже врезана в запись)
check-rules-mirror ........ файл с правилами отстал от базы (в общем прогоне)
check-stale-urgency ....... срочность горит, а вопрос закрыт
check-rule-basis .......... у правила свода не найти основание
measure-urgency-threshold . порог для срочности; РАЗЛИЧАЕТ разговор и цитату
measure-card-selfcontained  понятна ли задача без чата, где родилась
bite-phoenix-empty ........ приёмка защиты памяти роли
```

### ⬜ ЧТО ОСТАЛОСЬ — очередь перемерена по базе и коду 09.08 13:36 UTC
```
1️⃣ РАСКАТКА ОСТАЛЬНЫХ РОЛЕЙ — проба на PROTO ПРОЙДЕНА (разбор выше), следующая CORE
        📌 пробу берём САМУЮ АКТИВНУЮ, а не тихую: у молчащей дефект не проявится,
           и мы примем тишину за успех. Окно «двух миров» держать коротким — один
           рабочий заход, не сутки: пока идёт проба, часть ролей на новых правилах,
           часть на старых, и они будут спорить о том, что верно
        ⚠️ И то, что проба сделала ДО перехода остальных: CORE прошёл все семь секций
           своей памяти и нашёл ЧЕТЫРЕ отменённых приказа — один в той секции, которая
           читается ПЕРВОЙ при каждом рождении. Это работа каждой роли перед её пробой
2️⃣ 🟡 ПРИЁМКИ ИСПЫТЫВАЮТ НЕ ТУ КОПИЮ — самый опасный остаток, но уже НЕ ГОЛЫМИ РУКАМИ
        Было: все ~25 приёмок жёстко указывают на ЖИВЫЕ скрипты ⇒ зелёный прогон после
        переноса доказывает исправность ОРИГИНАЛА, а не шаблона.
        ✅ 09.08 построен ВЫБОР ИСПЫТУЕМОЙ КОПИИ — одно место, решающее, что испытывать,
           и запрет молча заимствовать из живого контура: приёмка, взявшая живой файл
           при заказанной шаблонной копии, ОТКАЗЫВАЕТСЯ («не запустилась»), а не зеленеет
        ✅ и шаблон стал самодостаточным: шаги схемы перенесены (их не было вовсе —
           приёмка прав молча брала описание таблицы ИЗ ЖИВОГО контура)
        🔴 остаток: переведена ОДНА приёмка из 26. Остальные по-прежнему смотрят в живое
        👉 главный случай приёмки: сломать скрипт В ШАБЛОНЕ, не трогая живой; прогон
           по шаблону обязан покраснеть, по живому — остаться зелёным
3️⃣ 🔴 ВИТРИНА «ТОЛЬКО МОЁ» ЛЖЁТ В ПУСТОЙ ВЕТКЕ — чужая находка, НЕ ПОЧИНЕНО
        ⚖️ предмет мой, а рука не моя: это живой инструмент координатора. Моё разовое
           разрешение править живые скрипты ПОТРАЧЕНО (видно запросом) ⇒ несу правкой
           на его руку или отдельным словом владельца, а не молча
        Когда лично обращённого нет, витрина печатает «Нет непрочитанных сообщений» —
        утверждение о ЛЕНТЕ, а не о подмножестве. Проверено по коду 09.08: строка общая
        для витрины и обычного чтения.
        🎯 Ветка, где механизм молчит по делу, — единственная, где он говорит неправду,
           и приходит она ровно тогда, когда роль склонна решить, что дочитала.
        ⚠️ И она спорит с правилом, записанным за восемь минут до неё: «витрина задаёт
           ПОРЯДОК чтения, а не заменяет его». Свод и механизм разошлись — роль поверит
           механизму: он отвечает ей лично и сейчас, а правило надо пойти и спросить
4️⃣ 2.1  пометка «замер или пересказ» — опора готова (слой «только от роли» создан 2.5),
        и форма опробована ТРИЖДЫ рядом: 'field' против 'backfill' у адресатов, поле
        «на каком основании» у правил, 'field'/'backfill' у прав роли
5️⃣ СБОРЩИК СОСТОЯНИЯ ДИСКА ждёт врезки — написан ЧУЖОЙ рукой (opssre), приёмка 8 случаев
        Машинный слой памяти знает БАЗУ и не знает ДИСКА — граница названа честно, но факты
        о репозиториях и живости сервисов роли всё равно хранят руками, и они врут так же.
        Цена уже заплачена: роль записала «докера нет», через 13 минут сама намерила обратное
✅ ЗАКРЫТО ЗА 08–09.08: 1.1 адресат полем · 1.3 срочность вычисляется · 2.2 основание полями
   (0 из 45 → 30 заполнено, БЕЗ основания 0 из 40 — включая 13 правил координатора) ·
   2.5 память роли · журнал изменений схемы · перенос в шаблон · права роли полями ·
   разгон сна · время в подпись ставит инструмент · выбор испытуемой копии · проба-канарейка
```

### ⏳ Отдельно, вне очереди идей
```
Перископ ......... ✅ ПОЧИНЕН 09.08: страница «Схема» больше не гаснет молча (расхождение
                   имени поля на ОДНО СЛОВО между службой и клиентом — ни ошибки сборки,
                   ни предупреждения: проверка типов верит объявлению, а не тому, что
                   приходит по сети). Правила сгруппированы по смыслу, отозванные — отдельно.
                   ⏳ Карточка #105 по-прежнему ждёт снимков живой страницы от владельца
мои долги ........ 19 открытых карточек (было 14), у ТРЁХ нет критерия ⇒ их нельзя закрыть.
                   Ломающая одна — сверка образца схемы с живой базой (#89).
                   ⚠️ #57 «употребление против упоминания» перестал быть теоретическим:
                   прибор нашёл 14 «протухших» правил, проверка построчно убила 6 из 7 —
                   там эти слова стоят в объяснениях «этого больше нет»
раскатка ......... ⛔ БОЛЬШЕ НЕ ЖДЁТ СЛОВА НА ОТПРАВКУ: правило снято. Шаблон сведён
                   с живым контуром, проверен на СВЕЖЕСОБРАННОМ контуре
долг ленты ....... ✅ у PROTO закрыт ЦЕЛИКОМ 09.08 13:32 UTC: 299 записок прочитаны
                   восемью батчами, каждый подтверждён склейкой. Постоянная проверка
                   «пишут, но не читают» зелёная
```

---

# 📖 НЕВЫПОЛНЕННОЕ — ПРОСТЫМИ СЛОВАМИ, БЕЗ ВНУТРЕННИХ СЛОВ
_Собрано 2026-08-09 10:35 UTC по запросу владельца, перемерено 13:36 UTC. Это НЕ пересказ
разделов ниже, а тот же список без жаргона: ниже он записан подробно и техническим языком,
здесь — так, чтобы понял человек, который систему в голове не держит._

## 🔴 НА МНЕ — И ПЕРВОЕ ОПАСНЕЕ ОСТАЛЬНОГО
```
① ПРОВЕРКИ ИСПЫТЫВАЮТ НЕ ТУ КОПИЮ. — 🟡 наполовину сделано сегодня
   Есть образец механизма (из него рождаются новые команды) и наш живой рабочий контур.
   Проверки всегда смотрят на ЖИВОЙ. Значит после переноса в образец зелёный результат
   говорит «оригинал исправен» — и НИЧЕГО не говорит про образец.
   ✅ Сегодня сделано ДВА из трёх: (1) появилось одно место, которое решает, какую копию
      испытывать, и оно ЗАПРЕЩАЕТ молча взять живую вместо заказанной; (2) образец стал
      самодостаточным — раньше в нём не было описаний устройства базы вовсе, и проверка
      тихо брала их из живого контура, оставаясь зелёной
   🔴 Осталось: перевести на этот выбор остальные 25 проверок из 26

⓪ 🔴 ВИТРИНА «ТОЛЬКО МОЁ» ГОВОРИТ НЕПРАВДУ, КОГДА ЕЙ НЕЧЕГО ПОКАЗАТЬ. — НЕ ПОЧИНЕНО
   Роль может спросить «что адресовано лично мне». Если лично ей не писали, витрина
   отвечает «Нет непрочитанных сообщений» — то есть говорит про ВСЮ ленту, хотя смотрела
   в свою узкую часть. Роль решит, что дочитала, и закроет чат с непрочитанным долгом.
   Нашла это ЧУЖАЯ роль своим прогоном через шесть минут после того, как я записал
   правило «витрина задаёт лишь порядок чтения и не заменяет его».

② ПОМЕТКА «ЭТО ИЗМЕРЕНО ИЛИ ПЕРЕСКАЗАНО».
   Сейчас факт в памяти роли выглядит одинаково, измерен он или пересказан со слов.
   А ломается почти всегда пересказ: он рождается уже устаревшим

③ ПРИБОР НЕ ОТЛИЧАЕТ «ДЕЛАЙ ТАК» ОТ «РАНЬШЕ ГОВОРИЛИ ДЕЛАТЬ ТАК».
   08.08 это чуть не стоило владельцу шести пустых вопросов: прибор нашёл 14 «протухших»
   правил, а проверка построчно убила 6 из 7 — там слова стояли в объяснениях

④ ПОСТОЯННАЯ ПРОВЕРКА НЕ ЧИТАЕТ ГЛАВНЫЙ ФАЙЛ-КАНОН.
   Его читает КАЖДАЯ роль при пробуждении, а проверки на него не смотрят вообще.
   Поэтому в нём почти сутки жил отменённый запрет, и нашли его глазами, а не прибором

⑤ МЕХАНИЗМ ПЕЧАТАЕТ ИМЯ, КОТОРОГО У НЕГО НЕТ.
   Инструмент подписывается или ссылается на то, чего в системе не существует.
   Читатель верит подписи

⑥ ФАЙЛ СО СВОДОМ ПРАВИЛ НЕ ПЕРЕСОБИРАЕТСЯ САМ.
   Правило меняется в базе, а человекочитаемая копия — отдельным действием.
   Забыли — копия учит старому

⑦ ОПИСАНИЕ УСТРОЙСТВА БАЗЫ РАЗОШЛОСЬ С САМОЙ БАЗОЙ (три таблицы).
   ⚠️ ЕДИНСТВЕННЫЙ ЛОМАЮЩИЙ пункт списка: починка меняет смысл уже написанных запросов
   у ролей и в просмотрщике. Нести владельцу ОТДЕЛЬНО и заранее

⑧ НОМЕРА СООБЩЕНИЙ И ЗАДАЧ — ОДНО ПРОСТРАНСТВО.
   «#135» может значить и сообщение, и задачу. Смягчили правилом «писать словом, что это»,
   но не вылечили

⑨ У ПОЧИНЕННОГО ЖУРНАЛА ИЗМЕНЕНИЙ БАЗЫ НЕТ ПРИЁМКИ.
   Сам журнал координатор починил 08.08. Но проверки, которая ЛОМАЕТ его нарочно и смотрит,
   заметит ли он, — нет. Писать её мне: условие приёмки писал я
```

## ⏳ ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА
```
⑩ РАЗОВЫЕ КОМАНДЫ ПОД ЗАДАЧУ. Под конкретную работу собирается временная группа ролей,
   делает и расходится. Наполовину так и живём; вторая половина меняет ТО, КАК ВЫ ВЕДЁТЕ
   РАБОТУ, поэтому решение ваше
⑪ СНИМКИ ЖИВОГО ПРОСМОТРЩИКА — карточка #105 ждёт ваших глаз с 07.08. Субагент снимок
   сделать не смог: панель браузера не рисует кадры
⑫ ⚰️ «кого берём следующей пробой» — СНЯТО: вы ответили 09.08 10:26 UTC, следующая CORE.
   Строка стояла здесь ожиданием, будучи уже решённой девятью минутами раньше, — тот же
   класс, за который я ловлю других: замер о состоянии протухает быстрее, чем пишется абзац
```

## 👤 НЕ МОЁ
```
⑬ ⚰️ ТРИНАДЦАТЬ ПРАВИЛ КООРДИНАТОРА без основания — ЗАКРЫТО ИМ САМИМ 08.08 вечером.
   Замер 09.08 13:36: правил без основания НОЛЬ из сорока живых. Оставляю строку
   надгробием, а не стираю: иначе через неделю кто-нибудь снова пойдёт их искать
```

---

# ЧАСТЬ 1. ИДЕИ, ГОТОВЫЕ И ВНЕСЁННЫЕ В РАБОЧУЮ БАЗУ

### 1.1 Записка знает, кому она адресована — ✅ ЗАКРЫТО ЦЕЛИКОМ 08.08
**Болело.** Адресат угадывался из текста. Из базы вопрос «что адресовано лично мне» **задать
нельзя** — только прочитать всё и понять глазами. Замер: при пробуждении роль читает 12–26
тысяч слов, лично ей адресовано от 1 до 20 %. У одной роли — 18 записок из 482.
**Доказано.** 13 проверок, 3 нарочные поломки пойманы; перенос прогнан на 1471 живой записке.
🪤 **Состояние 07.08 было «наполовину», и это оказалось верной оценкой:** поле говорило лишь
«адресация задана явно», а САМИ ИМЕНА оставались прозой ⇒ вопрос «покажи адресованное роли X»
задать было нельзя.

✅ **Вторая половина сделана 08.08 по слову владельца «сделай адресата полем».**
```
таблица имён адресатов ......... есть; у строки различаются ОБРАЩЕНИЕ и КОПИЯ
ручки --to / --cc .............. пишут ИМЕНА, а не «да, задана»
витрина --to-me ................ показывает только обращённое к вам
обратное заполнение ............ 5603 строки на 1015 записок (слово владельца 14:29 UTC)
```
📏 **Что это дало ролям** — долю ленты, которую можно НЕ ЧИТАТЬ: от 67 % до 98 %
(у одной роли 431 записка долга → 16 обращений).
⚖️ **Потолок назван и печатается при каждом прогоне, а не спрятан:** половина ленты
не разбирается никак — адресация выражена прозой без «собаки». Молчание витрины по такой
записке значит «механизму нечем отбирать», а НЕ «вам не писали».
🛡️ Разобранное машиной помечено `backfill`, объявленное человеком — `field`, и одним запросом
видно, чему верить. Смешать их значило бы дать догадке выдать себя за сказанное человеком.
🔴 **Живой замер привычки, 14:40 UTC:** имена ручкой объявили пока **три роли на 8 записок**
(PROTO 4 · CORE 2 · COORD 2). Сосуд работает, привычка ещё нет — мерить снова через сутки.

### 1.2 Честная отметка «докуда дочитано» — ✅ ВНЕСЕНО (шаг 003), живёт
**Болело.** Одно число не отличало три разные вещи:

| что произошло | было в базе | правда |
|---|---|---|
| прочитал глазами | «до N» | прочитал |
| разрешили пропустить | «до N» | **не читал, но это согласовано** |
| роли ещё не существовало | «до N» | **не пропускал — её не было** |

Из-за этого нельзя было ответить «что до роли не дошло». Одной роли пришлось **просить вслух**:
«если вы писали мне за семь недель — не дошло, повторите».
**Доказано.** 14 проверок, 5 нарочных поломок. Третий вид («роли не было») добавлен после
живого случая: механизм требовал «повторите запрос» у тех, кто новой роли никогда не писал.
**Состояние 07.08.** 9 отрезков с основанием — живёт.

### 1.3 Пометка «срочно» гаснет, когда вопрос закрыт — ✅ ПОСТРОЕНО 08.08, ходом Г (вычислять)
**Болело.** Срочность ставилась и не снималась никогда. Замер: **403 записки старше недели
несли «срочно»**. При этом реакция на «срочно» и на обычное была одинаковой (8,1 и 8,8 минуты)
— пометка уже ничего не сортировала, только мусорила.
⛔ Гашение по времени вы отклонили сами, и правильно: чаты останавливаются на сутки,
календарный срок погасил бы всё разом во время общей паузы.
**Доказано.** 8 проверок, 3 нарочные поломки.
🪤 **ПОПРАВКА ПРОТИВ СЕБЯ, 07.08 14:19 UTC.** В 13:55 я записал сюда: «механизм внесён,
ручка есть, погашена 1 записка из 537 — четвёртый случай „сосуд есть, никто не зовёт"».
**Это было неверно, и проверил я только когда взялся чинить.** Поле `resolved` и ручка
`--resolves` служат ДРУГОМУ: они помечают, что УТВЕРЖДЕНИЕ ОТМЕНЕНО. К срочности отношения
не имеют.
> **Я принял соседний сосуд за нужный, потому что имя подошло.**
Тот же класс, что урок ④ ниже: совпадение формы приняло себя за совпадение смысла.
⇒ Диагноз другой: не «не зовут», а **НЕ ПОСТРОЕНО ВОВСЕ**. И лечение поэтому другое —
уговаривать некого.

✅ **Построена проверка** (слово владельца 14:15 UTC: «даю добро на проверки»): находит
горящие срочные, на которые уже ответили, и называет их поимённо. Она ничего не гасит —
изменение живых данных решается отдельно и чужой рукой.

🎯 **ПОРОГ НАЗВАН ЗАМЕРОМ 07.08 16:39 UTC — это и было главное, чего не хватало для решения.**
Координатор предложил ход Г (не хранить, а вычислять) и отказался брать «молода» из головы.
```
                    доля ответов | срочные | обычные
                            50%  |  0,2 ч  |  0,2 ч     ← РАЗНИЦЫ НЕТ ВОВСЕ
                            95%  | 10,5 ч  | 19,3 ч
🎯 ПОРОГ 12 часов — накрывает 95,4 % всех ответов, которые срочная записка получает
⚖️ ЦЕНА: 20 ответов из 415 приходят позже — эти записки уже показывались бы обычными
```
🔴 **И то, чего никто не спрашивал: пометка не ускоряет ответ НИ В КАКОМ возрасте.**
Значит 12 часов — это не «когда срочность выдыхается» (она не начинала), а «когда ответа
уже можно не ждать». Владельцу это читать ПЕРВОЙ строкой, иначе выглядит, будто чиним работающее.

🔬 **Спор о числах разрешён различением, а не уступкой.** Координатор считал ответом только
проставленную связь (10 %), я — любую ссылку (72 %), и он написал: «точнее не скажет
ни твой инструмент, ни мой». Скажет:
```
узкая   только связь ..................................  56   9,7 %
🎯 средняя  связь + ссылка ВНЕ цитаты с названным автором  328  56,8 %  ← искомое
широкая любое «#N», включая цитаты .................... 415  71,9 %
без отклика вовсе ..................................... 162  28,1 %
```
⇒ 87 записок «широкой» полосы оказались ЦИТАТАМИ, а не разговором. Полоса неопределённости
была 62 пункта, стала 15. **Спор был не про аккуратность, а про то, что под словом «ответ»
у нас было два разных предмета.**
⚖️ Потолок печатается самой программой: ответ ПРОЗОЙ, без ссылки и связи, ей не виден ⇒
12 часов — ВЕРХНЯЯ оценка, а не точное число.

✅ **ИСПОЛНЕНО 08.08 — слово владельца 09:56 UTC: «срочность — вариант Б».**
Срочность больше НЕ ХРАНИТСЯ как признак, который кто-то обязан снять. Она **вычисляется
при каждом чтении** из трёх наблюдаемых вещей: пометка автора · возраст · был ли отклик.
```
горит ...... поставлено «срочно», отклика нет, младше 12 часов
выдохлось .. поставлено «срочно», старше 12 часов ЛИБО отклик уже был
```
🎯 **Почему это лучше гашения:** гасить — значит завести ещё одну обязанность, которую
забудут; ровно так умерли четыре наших механизма «по желанию» (замер: 0 вызовов из 1724,
0 из 84, 0 из 9, 1 из 546). **Хранить производный факт — значит дать ему врать. Не хранить —
ему нечем врать.**
📌 Вычислитель живёт в общем модуле контура, приёмка — 5 случаев, 4 различающих, у каждого
контрольная пара; прогнана на ЖИВОМ модуле, не на своей копии.

---

# ЧАСТЬ 2. ИДЕИ, ПРЕДЛОЖЕННЫЕ ДАЛЬШЕ

### 2.1 У факта есть пометка: это замер или пересказ — ⬜ НЕ ПОСТРОЕНО
**Болит.** Координатор вложил в новую роль пять фактов об устройстве ядра. Четыре были
замерами, один — пересказом по памяти. **Сломался ровно пересказ, в трёх местах сразу.**
Новая роль нашла это за первый час, потому что перемерила ту часть, на которую опиралась.
**Суть.** Замер стареет постепенно и честно. Пересказ **рождается старым** — расхождение
существует уже в момент записи.
**Предлагается.** У каждого факта пометка «замер» (кем, когда, чем мерено) или «пересказ»
(с чьих слов). Пересказ вкладывать можно, но он приходит с меткой «перемерь перед опорой».
⚠️ Проверкой не подтверждено: продумано на бумаге, работающего образца нет.
📌 **Опора под этот пункт появилась 08.08:** слой «только от роли» создан (см. 2.5), и пометка
ляжет именно в него. Плюс форма уже опробована дважды в соседних местах — у адресатов
(`объявлено человеком` против `разобрано машиной`) и у правил (поле «на каком основании»).
⇒ Это перестало быть новой идеей и стало ПЕРЕНОСОМ уже работающего различения на третье место.

### 2.2 Основание решения хранится полями, а не прозой — ✅ ЗАКРЫТО (09.08: БЕЗ основания 0 из 40)
**Болит.** Координатор сдвинул отметку о прочтении новой роли и записал ВСЁ ПРАВИЛЬНО:
основание, способ, следствие, «ложного подтверждения не ставилось». **Прозой, в свободном
поле.** Замер по всей истории: таких записей с основанием — **одна**.
**Суть.** Форма не воспроизводится. Каждый следующий восстанавливает её по памяти о чужом
поступке. Механизм нужен **не чтобы заставить, а чтобы не приходилось помнить**.
**Предлагается.** Поля: чем обосновано, кто разрешил, какой запиской объявлено.

⛔ **ОТОЗВАНО МНОЙ ЖЕ 07.08 16:29 UTC — прежний статус был неверен.** Он гласил: «для одного
вида решений поля заполнены у 9 отрезков из 9, форма работает и воспроизводится, осталось
перенести». Перемер показал другое: **девять строк — это ОДНА миграция 06.08, разложенная
по девяти ролям.** У всех девяти одинаковый текст основания, одинаковое «разрешил владелец»,
а поле «какой запиской объявлено» **пусто у всех девяти** — то есть третье поле не
использовано ни разу. Я посчитал РЯДЫ и назвал это работающим образцом.
> 🪤 Ложная единица: счёт строк принял себя за счёт решений. Переносить нечего — образца нет.

🔴 **Состояние 07.08 17:37 UTC, замер по своду правил** (`vnext-tools/check-rule-basis.py`,
приёмка — 8 случаев, 7 различающих, у каждого контрольное слепое правило):
```
правил всего .............................. 45
названо КТО решил ......................... 26
названо КОГДА (дата рядом с припиской) .... 19
названо ГДЕ (ссылка на записку) ...........  2
названы все три ...........................  2
🔴 основание не найти ВОВСЕ ............... 19
   из них с замком ВЛАДЕЛЬЦА .............. 13
```
⛔ **ПЕРВЫЕ ЧИСЛА ЭТОГО ЗАМЕРА (26 из 44, 15 с замком) БЫЛИ ЗАВЫШЕНЫ НА ЧЕТВЕРТЬ, отзываю.**
Проверка признавала основанием ИСКЛЮЧИТЕЛЬНО «слово владельца» и объявляла слепыми правила,
основанные на замере коллеги. Семь правил обвинены зря. Нашлось так: координатор завёл правило
`dowry-facts-carry-source` с образцовым основанием (кто · когда · где) и НАМЕРЕННО без замка
владельца — а моя проверка назвала его слепым.
> 🪤 **Проверка отвергла ровно то правило, которое написано по её же требованию** — то есть
> учила, что выписывать основание бесполезно. Третий за сутки случай «проверка красная на верном».
✅ Починено с границей: основанием считается роль, названная РЯДОМ С ПРИМЕТОЙ ПРИПИСКИ
(«замер @RCC», «внесено рукой»), а процедурное упоминание («правку вносит COORD») — НЕТ.
На это заведён отдельный различающий случай: без него правка сделала бы проверку беззубой.
⚖️ **Цена.** Правило с замком владельца объявляет, что менять его вправе только он.
Пятнадцать таких не говорят, КОГДА и ГДЕ он это решил ⇒ отозвать нельзя безопасно
(отзывающий не знает, отменяет он живое решение или чей-то пересказ), проверить нельзя вовсе,
а роль, читающая свод при пробуждении, не отличит решение от привычки.
⇒ Предмет не «перенести», а **завести форму там, где её нет, начиная со свода правил** —
и это решение о схеме живой базы: рука координатора, слово владельца. Я предлагаю, не правлю.

✅ **ФОРМА ЗАВЕДЕНА 08.08 — слово владельца 09:56 UTC: «основания — вариант А»,
и его же добавление: «рассмотри поле „условие отмены“».** Полей стало ПЯТЬ:
```
на каком основании ..... замер, случай, довод — почему правило вообще существует
кто разрешил ........... владелец / координатор / роль
где это сказано ........ номер записки или чат с временем — чтобы можно было пойти и прочесть
ВИД условия отмены ..... бессрочно · до даты · до события · пока держится замер
деталь условия ......... сама дата, само событие, само число и чем мерено
```
🎯 **Четвёртое поле — ваше, и оно оправдалось прежде, чем было построено.** Я проверил его
ценность на наших же надгробиях: **шесть правил умерли оттого, что условие их отмены УЖЕ
НАСТУПИЛО, и никто этого не заметил** — до ручного разбора недели спустя.
⚠️ **Вид отмены сделан списком, а не прозой,** иначе у всех выродилось бы в «бессрочно»:
вид можно ПОСЧИТАТЬ и показать распределение, прозу — только прочитать глазами.
🛡️ При сохранении правила без основания механизм **ОТКАЗЫВАЕТ**, а не ворчит: необязательное
поле у нас умирало четыре раза подряд.

🔴 **ЧЕСТНОЕ СОСТОЯНИЕ НА 08.08 14:40 UTC — СОСУД ПУСТ, И ЭТО ГЛАВНАЯ ОСТАВШАЯСЯ РАБОТА:**
```
правил в своде .............................. 45
заполнено «на каком основании» ..............  0
заполнено «кто разрешил» ....................  0
заполнено «где сказано» .....................  0
заполнено «вид условия отмены» ..............  0
🔴 правил сохранялось после врезки ..........  0   (последняя правка правила — 07.08 17:30)
```
⇒ Отсюда два следствия, и оба надо называть вслух:
**①** свод правил СЕЙЧАС отвечает на «почему это правило живо» ровно так же, как до врезки;
**②** ОТКАЗ при сохранении **ни разу не сработал на живом** — он проверен только приёмкой.
Приёмка на своей копии не доказывает ничего про чужое поведение.

## ✅ ЗАКРЫТО ЗА ВЕЧЕР 08.08 — СОСУД НАПОЛНЕН, И ОТКАЗ ПРОВЕРЕН НА ЖИВОМ
Первое, что я сделал, — **попробовал записать правило без объяснения**, чтобы узнать, работает
ли защита вообще:
```
⛔ ПРАВИЛО НЕ СОХРАНЕНО: правило свода без объяснения — приказ без причины.
   код возврата 2 · в базу НЕ попало
```
📏 **Числа на 08.08 23:32 UTC** (запрос к базе, не пересказ):
```
записей в своде ......... 50    надгробий 10    ЖИВЫХ 40
основание ПОЛЯМИ ........ 27 из 40   ← было 0 из 45 утром
замок владельца ......... 23 живых · БЕЗ ОСНОВАНИЯ 0   ✅ закрыто
замок координатора ...... 17 живых · без основания 13   ← его зона, несёт сам
```
📏 **Перемер 09.08 13:36 UTC — сторона координатора закрыта им самим за ночь:**
```
записей в своде ......... 50    надгробий 10    ЖИВЫХ 40   ← состав не менялся
основание заполнено ..... 30
🎯 БЕЗ ОСНОВАНИЯ ......... 0 из 40 живых — по ВСЕМУ своду, а не по одной стороне
```
🎯 **КАК ЭТО БЫЛО СДЕЛАНО — форма важнее чисел, потому что она воспроизводима.**
Не «дай основание», а: правило пересказать ЯЗЫКОМ ВЛАДЕЛЬЦА · назвать, чего я не знаю ·
дать рекомендацию **с ценой** · спросить условие отмены. Ответ занимал у него одну строку;
за вечер так прошло 24 правила по два-три за ход.
📌 И проверка формы: **трижды он ответил НЕ ТО, что я рекомендовал** — значит форма
не подсказывает ответ, а спрашивает.

⚡ **ЧЕТВЁРТОЕ ПОЛЕ ВЛАДЕЛЬЦА ОКУПИЛОСЬ СРАЗУ.** «Условие отмены» вскрыло целый класс:
правила, чьё условие УЖЕ наступило, а никто не заметил. За вечер найдено ТРИ таких —
«молчит один такт» (пульса ролей нет), «фаза перехода действует» (переход кончился
три недели назад), «в простое без задач» (простоя не бывает).
> **Условие, опирающееся на умерший механизм, не краснеет. Оно молча становится вопросом
> вкуса — а правило продолжает выглядеть строгим.**

### 2.3 Постоянная проверка не считает своими записками долг роли — ✅ СДЕЛАНО 06.08
**Болит.** Проверка «пишет, но не читает» считает непрочитанное как «конец ленты минус
отметка», не различая автора. Но цикл работы (*прочитал → подтвердил → написал*) **по
построению** оставляет свою свежую записку непрочитанной.
На срабатывании: непрочитанных 10, своих из них 2, порог — больше 8. **Две свои записки
решили исход.**
✅ **Состояние 07.08 14:27, перемерено по коду:** проверка считает по ЧУЖИМ записям
(`writer_role <> role`). Починено 06.08 по этому же замеру.
📌 И довод в починке шире исходного: прежняя формула была строже всего к тому, кто больше
работает — чем активнее роль пишет, тем ниже для неё фактический порог. Строгость при этом
НЕ упала: в том случае чужих было ровно 8, дочитывать надо было и так. Изменился ПРЕДМЕТ
счёта, а не порог.
🪤 В редакции плана от 14:00 здесь стояло «⬜ не сделано» — статус протух на сутки.
Поймано только тем, что перед выбором очереди я перемерил, а не поверил своему же документу.

### 2.4 Пробуждение читает оглавление, а не все тексты — ✅ СДЕЛАНО 06.08
**Болело.** Роль при пробуждении получала полные тексты записок.
**Замер.** Записок 1564, серединная длина 2113 знаков, самая длинная 10938. Оглавление плотнее
полных текстов в 17,6–28,5 раза. Не прочитано было: у одной роли 102 записки = 407 КБ,
у второй 95 = 379 КБ, у третьей 87 = 349 КБ. Итого по контуру 1229,6 КБ текстами → 43,0 КБ
оглавлением, экономия 97 %. Заголовок пригоден у 100 % записок.
**Сделано.** Сначала оглавление (кто, когда, срочно ли, адресовано ли мне, о чём), тексты —
по запросу. На живой ленте: 105 непрочитанных = 418 КБ текстами против 14 КБ оглавлением.
🔴 **Главное решение здесь — не про экономию.** Чтение оглавления **не двигает отметку
«дочитал»**. Иначе оглавление не облегчение, а способ незаметно пропустить ленту: роль
«прошла» сотню записок, не прочитав ни одной, и система считает их прочитанными.
Кнопки «подтвердить всё из оглавления» нет и не будет.

### 2.5 Память роли разделена по происхождению — ✅ ВРЕЗАНО В ЖИВОЙ ИНСТРУМЕНТ 08.08
**Болит.** Память роли — смесь трёх разных вещей: что машина соберёт сама; что знает только
роль; что не меняется. Оттого она и самая большая, и самая лживая: устаревает та часть,
которую машина пересобрала бы за секунду.
**Предлагается.** Три слоя: **собираемое машиной** (протухнуть не может — пересобирается при
каждом чтении), **только от роли** (намерения, границы, чего ждёт), **неизменное**.
**Доказано.** Два теста на реальных случаях: смерть роли без сохранения и затёртый раздел.
📏 **Перемер 07.08 14:27** — память ВЫРОСЛА, и это довод в пользу срочности:
```
было 06.08 ..... 184 КБ         стало 07.08 ..... 259 КБ (264 887 знаков)  +40 % за сутки
разделов ....... 7, но они ТЕМАТИЧЕСКИЕ (кто я · план · история · чем разбудить · …),
                 а НЕ по происхождению. Поля «откуда это знание» нет вовсе
крупнейшие ..... состояние 62 КБ · перерождение 47 КБ · план 40 КБ
```
🎯 Это читает КАЖДАЯ роль при КАЖДОМ пробуждении. Слой «собираемое машиной» протухнуть
не может по построению — значит он же и снимает бóльшую часть класса «память отстала
от работы», не требуя дисциплины.

📏 **Перемер 07.08 18:48 UTC:** 63 секции, 266 692 знака (260 КБ). Рост за смену остановился —
но не потому, что стало лучше: половина ролей провела вечер в чужих зонах и своей памяти
не касалась.
🔴 **И за эту смену память дважды показала, что она хрупкая, — поэтому пункт поднят в очереди
на ПЕРВОЕ место:**
```
① @RCC обнулил секцию скриптом, упавшим посреди записи. Инструмент принял пустоту
   и отчитался «OK (0 chars)». Секция, которая учит преемника порядку пробуждения,
   пролежала пустой четыре минуты
   ⚠️ и заметить было нечем: постоянная проверка живости смотрит на ВРЕМЯ сохранения, а не на РАЗМЕР ⇒
      обнулённая память выглядит СВЕЖАЙШЕЙ
   ✅ починено: пустое тело — отказ всегда; обвал в разы — отказ без явного слова;
      вывод печатает «было N → стало M» (одно число можно не заметить, разницу — нет)
② проверка ⑥ гасилась ПЕРЕСОХРАНЕНИЕМ: достаточно нажать «сохранить», ничего не изменив
   ✅ починено наполовину: пока механизм правится, случай откладывается и отсрочка
      печатает свой размер. ⛔ Полное лечение — сравнивать не ВРЕМЯ, а СОДЕРЖАНИЕ,
      и это строка в живой базе ⇒ слово владельца
```
⇒ Оба случая — про ОДНО: **память роли меняется без следа и без свидетеля.** Разделение
по происхождению закрывает это не дисциплиной, а построением: слой машины пересобирается,
слой роли мал и потому обозрим.

✅ **СДЕЛАНО 08.08 — слово владельца: «разрешаю врезку в живой инструмент для работы памяти».**
Слой «собираемое машиной» теперь **не хранится вовсе**: он пересобирается из базы в момент,
когда роль читает свою память. Протухнуть ему нечем — хранить нечего.
```
что роль видит, просыпаясь, СВЕЖИМ — а не «каким записали вчера»:
  долг ленты числом
  ОБРАЩЕНИЯ лично к ней — отдельно от копий
  свежее ли её собственное последнее слово, чем её же слепок
  какие правила свода изменились с последнего пробуждения
```
🛡️ **Названная граница вместо молчаливой полноты:** блок вслух говорит, что знает БАЗУ
и не знает ДИСКА. Неназванная граница читается как полнота — формулировка координатора,
точнее моей.
🛡️ **Частичный отказ печатается строкой, а не роняет пробуждение:** если слой не собрался,
роль читает «НЕ СОБРАН», а не пустоту, которую легко принять за «всё в порядке».
✅ **Принято ЧУЖОЙ рукой, и это здесь главное.** Я держал задачу открытой, пока другая роль
не скажет, что блок ей ДАЛ. Координатор ответил по существу, а не вежливостью:
> «личное обращение я бы увидел только на следующем такте, а блок показал его в момент
> чтения слепка»

⇒ **Свой инструмент, проверенный только собой, был половиной приёмки.**
📌 Остаётся перенос этого в шаблон — карточка #129, порядок жёсткий: сначала схема полей,
потом основания, потом машинный слой.

### 2.6 Ответ связан с вопросом полем, а не прозой — ✅ ВНЕСЕНО (шаг 004), живёт
**Болело.** **83 % наших записок ссылались друг на друга прозой** — 1413 ссылок, 3,7 на записку.
Связь видел только человек. «Какие вопросы ко мне без ответа» было не запросом, а чтением глазами.
**Состояние 07.08.** **86 связей.** Прижилось само: номер записки и так писали прозой —
механизм лёг на существующую привычку.

### 2.7 Критерий приёмки у задачи + связь записки с задачей — ✅ ВНЕСЕНО (шаг 005), живёт
**Болело.** 39 открытых задач, **ни у одной проверяемого условия закрытия** — «готово»
решалось мнением. Обсуждение задачи было размазано по ленте.
**Состояние 07.08.** Критерий стал ОБЯЗАТЕЛЕН по вашему слову (см. ЧАСТЬ 6). Связь записки
с задачей — **34 строки**; ручка существовала всегда и не звалась ни разу до сегодня.

### 2.8 Признак «команда в памяти роли старше правки механизма» — ✅ СДЕЛАНО 06.08
**Болело.** У роли соседнего контура в памяти лежали **девять протухших команд, две из них
нарушали прямой запрет владельца первым же действием** новой сессии. Механика: автор годами
правит **описания** и не трогает **команды**.
> **Автор своей поверхности систематически слеп к её исполняемой части.**
**Состояние.** Работает; на нашем контуре **нашёл настоящее протухание**: в памяти роли стояло
«11 проверок», а их 13.

### 2.9 Отклонено или под вопросом — с основанием
| предложение | почему не берём |
|---|---|
| реакции-подтверждения одной строкой | довод **не подтвердился** у нас: 28 % записок несут подтверждение, но серединная такая записка — **2540 знаков**. Это разбор, а не «принято»; реакция его не заменит |
| реестр живых сессий (защита от двойника) | **ни одного случая** за 1526 записок. Страховка от чужого прецедента, а не лечение нашей боли |
| срочность выводить из структуры | направление верное, но похожую задачу я уже отменял перемером: норма сработала сама, инфляция срочности упала с 33 % до 10 %. **Строить механизм там, где норма работает, — платить дважды** |
| отменить пульсы у ролей | у нас они есть и работают — прямо противоречит нашей практике |

### 2.10 Разовые команды под задачу — 🎯 РЕШАЕТЕ ВЫ
Постоянен только координатор; под задачу собирается команда из 2–4 ролей плюс критик,
работает, закрывается запиской передачи и распускается.
⚠️ **Мы уже наполовину так живём:** роли просыпаются, когда вы открываете чат, а не по
расписанию. Вторая половина — команда под задачу и роспуск по достижении цели — меняет **то,
как вы ведёте работу**. Цена ложится на вас, поэтому и решение ваше.
📌 Следствие, которое стоит знать заранее: разовые команды **усилят** вашу боль из ЧАСТИ 4 —
больше запусков, больше решений, принятых в чате и не записанных в контур.

---

### 2.11 Права роли — полями, а не прозой в её памяти — ✅ ПОСТРОЕНО 08.08
**Болело.** Что кому разрешено и с чьего слова — лежало прозой в памяти каждой роли, своими
словами. Ровно в том виде, из которого днём пришлось выкапывать основания правил.
📏 **Цена заплачена дважды за одну смену:** снятие запрета на отправку жило в ТРЁХ местах
(два нашлись только потому, что искали), а разовое разрешение я сам **дважды не смог
вспомнить — потрачено оно или нет**.
**Сделано.** Четыре поля владельца — что · кто · когда · разовое или стоячее — плюс два,
без которых они не работают: **где сказано** (иначе право нельзя ни проверить, ни безопасно
отозвать) и **область** (класс дня: разрешение без названной области расползается).
⚡ **И одно, чего в замысле не было: разовое право ТРАТИТСЯ, и это видно запросом.**
> Разовое разрешение без следа расхода — это стоячее разрешение, которым пользуются,
> пока не постесняются.
⛔ Отзыв — не удаление: с причиной, иначе отвергается. Отозванное право спрашивают ровно
тогда, когда что-то пошло не так. Приёмка 10 случаев, 8 различающих.

### 2.12 Сколько спать до следующего синка — решает одна функция — ✅ ПОСТРОЕНО 08.08
**Замысел владельца:** тишина → сон растёт на 5 минут, потолок 50, и «должно работать как
код, а не как правило, про которое могут забыть».
**Взято целиком, но три вещи сделаны иначе — каждая закрывает ТИХИЙ отказ:**
```
① тишину МЕРИТ ФУНКЦИЯ, а не роль: иначе признак становится мнением усталой роли
② состояние хранит функция, а не вызывающий: «присылайте прошлое время» дало бы восемь
   хранилищ у восьми ролей, и первый перезапуск чата обнулял бы разгон МОЛЧА
③ сброс назван явно (любая чужая записка): неопределённый сброс каждый понял бы по-своему
```
⚖️ **Цена в правиле названа вслух:** на потолке срочная записка ждёт до 50 минут.
👉 Звать не надо — строка печатается сама в конце чтения ленты. ⛔ И стоит ПЕРЕД ключом
подтверждения: вторая половина ключа обязана остаться в последней строке.
> **Удобство не имеет права двигать чужой инвариант.**

### 2.13 Время в подпись ставит инструмент, а не роль — ✅ ПОСТРОЕНО 08.08
**Болело.** Правило «время только UTC» написано подробно и известно всем — и за один день
**две роли за два часа** подписали записки местным временем под буквами «UTC» (+2 часа).
Обе поймали себя сами. **Класс живёт при верном и известном правиле.**
⚖️ И это не мелочь: **ошибку во времени нельзя поймать чтением** — «16:51 UTC» выглядит
безупречно, расхождение видно только сверкой с базой. А неверное время тихо ломает
восстановление порядка событий, то есть ровно то, ради чего лента и ведётся.
**Сделано.** Одно и то же значение идёт в подпись и в колонку времени ⇒ разойтись не могут
по построению. ⛔ Цитаты чужих меток НЕ трогаются: механизм, «исправивший» цитату, подделал
бы её незаметно — в приёмке ЧЕТЫРЕ различающих случая из шести именно про то, чего трогать нельзя.

### 2.14 Шаблон не отстаёт от живого контура — ✅ СВЕДЕНО 08.08, ⚠️ наполовину
📏 **Замер до:** пяти механизмов в шаблоне НЕТ ВОВСЕ, схема не знает ни одного нового сосуда,
`write-message` в шаблоне **в 26 раз меньше** живого. Контур из шаблона собирался вчерашним
и выглядел исправным: приёмки шаблона проверяют то, что в шаблоне ЕСТЬ.
⚠️ Копилось молча по одной причине: у пары «инструменты ↔ шаблон» сторож дрейфа был,
у пары «живые скрипты ↔ шаблон» — **нет**.
**Сделано.** Схема ГЕНЕРИРУЕТСЯ из живой базы (рукописная — вторая копия правды, а вторая
копия расходится молча). Перенос и сторож — ОДИН инструмент: сравнение идёт после
детерминированного приведения путей, иначе сторож был бы вечно-красным. Сведено 16 файлов.
🎯 **Три дефекта нашла именно СБОРКА СВЕЖЕГО КОНТУРА, а не чтение файлов:**
```
① перенос выглядел полным — и на чужой копии НЕ ЗАПУСТИЛСЯ: не хватило модуля.
   Список писала рука, зависимости знает код ⇒ теперь замыкание считает механизм
② сборка печатала «схема v2», применяя v3 — впечатанное число врало уверенно
③ проверка ссылок искалась по ОДНОМУ пути, верному только в нашем контейнере
```
🔴 **НАПОЛОВИНУ — и это названо, а не спрятано:** приёмки по-прежнему указывают на ЖИВЫЕ
скрипты, значит зелёный прогон доказывает исправность ОРИГИНАЛА, а не шаблона.

✅ **ДОДЕЛАНО 09.08 — две трети остатка закрыты, третья названа числом.**
```
① ВЫБОР ИСПЫТУЕМОЙ КОПИИ живёт в ОДНОМ месте, а не в каждой приёмке отдельно.
   Раньше «какую копию проверяем» было записано в каждой из 26 приёмок своей строкой ⇒
   перевести их значило бы 26 раз повторить одно решение и 26 раз ошибиться по-своему
② ЗАПРЕТ МОЛЧАЛИВОГО ЗАИМСТВОВАНИЯ: если заказана шаблонная копия, а инструмент взят
   из живого контура — приёмка ОТКАЗЫВАЕТСЯ («не запустилась»), а не зеленеет.
   Без этого проверка шаблона проходила бы на живом коде и называла бы это успехом
③ ШАБЛОН СТАЛ САМОДОСТАТОЧНЫМ: в нём НЕ БЫЛО описаний устройства базы ВОВСЕ (7 штук),
   и приёмка прав молча брала описание таблицы ИЗ ЖИВОГО контура — оставаясь зелёной
🔴 остаток: переведена ОДНА приёмка из 26 (карточка #149)
```
🎯 Проверено не заявлением: **приёмка самого выбора ломает разбор параметра у каждого
механизма по очереди** — шесть механизмов, слепых ноль. Сначала она зеленела, ничего
не проверив (файл в копии был, и ветка «взято из живого» не выполнялась) — вскрыто
нарочной поломкой, а не чтением.

---

# ЧАСТЬ 3. МОИ ДОЛГИ — дыры, которые я назвал сам

| что | состояние 09.08 13:36 |
|---|---|
| 🔴 **витрина «только моё» лжёт в пустой ветке** — говорит про всю ленту, глядя в подмножество | ⬜ не начато. Нашла ЧУЖАЯ роль прогоном через 6 минут после того, как я записал правило «витрина не заменяет чтения». Подтверждено по коду 09.08: ветка общая с обычным чтением |
| 🔴 **проверка форм вызова не видит команд чужих инструментов** — ловит вызовы наших скриптов и пропускает `git -C C:\…` с теми же слэшами | ⬜ не начато. Нашёл CORE **глазами ПОСЛЕ зелёного прогона**: одна форма, два инструмента, оба «зелены». Предложение его и opssre: судить любой путь `C:\…` в строке, похожей на команду |
| ⏳ **сборщик состояния диска ждёт врезки** в собираемый машиной слой памяти | написан ЧУЖОЙ рукой (opssre), приёмка 8 случаев, отправлен. Врезка моя: слой знает БАЗУ и не знает ДИСКА, и факты о репозиториях роли по-прежнему хранят руками |
| ⏳ **не отвечено STUD**: какая его доля в апгрейде (он назвал три места, где его сегодняшний опыт ложится прямо) | висит с 08.08 15:55 UTC. Я обещал ответ отдельной запиской и не дал — **дольше всего висит именно то, что обещано «отдельно»** |
| постоянная проверка не читает главный файл правил проекта | ⬜ не начато (карточка #56). ⚠️ Предмет подтвердился делом: 08.08 в этом файле почти сутки жил ОТМЕНЁННЫЙ запрет на отправку, нашли его две роли глазами, а не прибор |
| прибор не отличает **употребление** от **упоминания** (строка «6 ролей из 8» прочлась как утверждение о составе; 5 подтверждений) | ⏳ **применено уже втрое:** ссылки · полосы отклика (87 записок оказались цитатами, а не разговором) · разбор адресатов (цитата «> @РОЛЬ» адресатом не делает). Общего решения по-прежнему нет (#57) |
| номера записок и задач — одно пространство, «#N» неразличим (все 62 задачи имели номер существующей записки) | ⏳ **смягчено:** правило требует «карточка #N» / «записка #N». Переименовывать задним числом вредно — лечим только вперёд (#63) |
| образец схемы разошёлся с живой базой | ⬜ не начато. Предмет узкий: три таблицы. **Отзыв правила — единственный ломающий шаг**, понесу отдельно (#89) |
| журнал изменений схемы перестал пополняться (найдено 08.08 14:40: база говорила «шагов 7, сверх рубежа 0» при четырёх применённых изменениях) | ✅ **ПОЧИНЕНО КООРДИНАТОРОМ 08.08 15:08**, в запрошенном порядке: сперва обёртка «шаг пишет себя сам» + отпечаток структуры, потом сторож, потом четыре записи задним числом. Проверено мной по следам: база отвечает «14 шагов», сторож в общем прогоне зелёный, **моя миграция воспользовалась его обёрткой через час после её появления**. ⚠️ Остаток: приёмки с нарочной поломкой у журнала НЕТ ⇒ карточка #135 не закрыта. Писать её мне: критерий приёмки писал я, принимать по чужому рапорту нельзя |
| механизм печатает имя, которого у него нет (справки и подписи) | ⬜ не начато (#87) |
| файл со сводом правил не пересобирается сам при правке правила в базе | ⬜ не начато (#108), перенос в шаблон — #110 |

---

# ЧАСТЬ 4. ЧЕГО В ЭТОМ СПИСКЕ НЕТ — и это важнее всего

Список собран **из того, что болело у нас**. Он не отвечает на вопрос, чего не хватает ВАМ:
что вы не видите, о чём приходится спрашивать, что узнаёте последним.

📌 Случай, где это уже проявилось: решение, принятое в вашем чате, **полтора часа
не существовало для контура** — координатор пятнадцать раз доложил вам «ждём вашего ответа»,
уже имея его.

👉 Дополняйте отсюда. Ваши пункты я разложу на части, оценю цену каждой и принесу порядок
работ с обоснованием — не список.

---

# ЧАСТЬ 5. ПОРЯДОК РАБОТ И ДОСКА СОСТОЯНИЯ

> **Слово владельца 2026-08-06 14:32 UTC:** «согласен с твоим планом работ: ш1, ш2, ш3, ш4, ш5,
> затем накат на рабочую базу. затем приступай к 2.8+2.4. к остальным пунктам вернёмся, когда
> выполним предыдущие и ты обновишь статус плана.»
> **И отдельное требование:** статусы обновлять **по завершении каждого этапа**, не в конце.

### Порядок и его основание
Порядок задан **не пользой, а зависимостями и ценой окна**: пока база меняется, дешевле сделать
всё, что её касается, одним заходом. Второй заход по той же таблице — второй риск и второе окно,
когда контур стоит.
```
Ш1  реестр ролей ............... опора: на неё ссылаются 5 из 8 новых таблиц
Ш2  адресат: 2 колонки ......... старое помечается «перенесено», не притворяется новым
Ш3  честная отметка отрезками .. старое значение переезжает одним отрезком «вид неизвестен»
Ш4  связь ответа с вопросом .... [идея 2.6, замер 83 %]
Ш5  критерий приёмки + связь записки с задачей          [идея 2.7, 39 задач]
    ↓  НАКАТ НА РАБОЧУЮ БАЗУ
2.8 признак «команда старше правки механизма»
2.4 оглавление вместо текстов при пробуждении
    ↓  найдено уже после наката, в плане не было
Ш6  инструмент записи учится тому, что уже наложено
Ш7  читалка ленты отмечала больше, чем показала
```

### 📊 ДОСКА СОСТОЯНИЯ
В графе «чем доказано» стоит **проверяемая вещь**, а не слово «готово». Слово «готово» проверить
нельзя, прогон — можно. Пустая графа означала бы «не подтверждено ничем», и это тоже статус.

| этап | что | состояние | чем доказано |
|---|---|---|---|
| **Ш1** | реестр ролей | ✅ внесён в базу | шаг `001-roles`; сухой прогон + прогон на копии, 3 отказа защиты. Сейчас: 11 ролей, 9 со статусом |
| **Ш2** | адресат полем | ✅ внесён | шаг `002`; все старые записки помечены «перенесено». Сейчас 89 помечены «адресация задана» |
| **Ш3** | честная отметка «докуда дочитано» | ✅ внесён | шаг `003`; 14 проверок, 5 нарочных поломок. Сейчас 9 отрезков с основанием |
| **Ш4** | связь ответа с вопросом | ✅ внесён | шаг `004`; 15 проверок, 6 нарочных поломок; прогон на копии: 1558 записок, таблица встала. Сейчас 86 связей |
| **Ш5** | критерий приёмки + связь записки с задачей | ✅ внесён | шаг `005`; 18 проверок, 6 нарочных поломок. Сейчас 34 связи; без критерия 22 задачи |
| **накат** | перевод рабочей базы | ✅ **СОСТОЯЛСЯ 06.08 15:16:23 UTC** | журнал в базе: все 5 шагов. Проверено ПОСЛЕ и по самой базе, а не по отчёту: закрытые роли не воскрешены · 1569 записок помечены «перенесено» · отметки переехали отрезками с основанием |
| **006** | номер версии считается из журнала | ✅ внесён | база отдаёт «версия 3, шагов 8, сверх рубежа 0». Было записано «1.0» при второй, а затем третьей схеме — **номер отставал на две версии и выглядел достоверным** |
| **007** | подсветка задач без критерия | ✅ внесён · долг убывает | готовый запрос в базе. Было 25 из 41 → сейчас 22 из 61 при вдвое большем числе задач |
| **2.8** | признак «команда старше правки» | ✅ работает на живых данных | самопроверка 4/4; нашёл настоящее протухание: «11 проверок» вместо 13 |
| **2.4** | оглавление при пробуждении | ✅ работает на живой ленте | самопроверка, 5 случаев, 2 встречных; 105 непрочитанных = 418 КБ текстами против 14 КБ оглавлением |
| **Ш6** | инструмент записи учится новым полям | ✅ три пункта из четырёх · ⛔ один ждёт вашего слова | ручки к трём сосудам врезаны и прижились; критерий пишется в поле; проверка «механизм отвечает?» работает. **Не сделано: пометка «перенесено» перестаёт доставаться новым записям** |
| **Ш7** | читалка отмечала больше, чем показала | ✅ **ЗАКРЫТ 07.08 10:05 UTC** | было: показано до записки #3081, отметка уехала на записку #3109 — 28 записок молча. Стало: отметка встала на записке #3081, встречный случай прошёл. Добавлено поле «сколько показано» и пометка подтверждения — запись больше не стирается |
| **проверка** | образец схемы против переноса | ✅ краснеет на подлоге | 14 проверок, 4 нарочные поломки; строит базу двумя путями и сверяет. Заведена потому, что готовый запрос был в образце и НЕ в переносе — трое говорили о несуществующем |
| **пост. проверка** | раздел памяти роли отстал от её работы | ✅ работает на живом контуре | 18 проверок; числа сошлись с проверкой координатора до штуки (6 позиций), при этом мой замер опроверг его порог «трое суток» — планы протухают за часы |
| **Перископ** | состояние видно глазами в браузере | ✅ работает локально · ⏳ доработка идёт | база открывается ТОЛЬКО на чтение (три замка); 8 колонок, сортировка, фильтры со счётчиками, панель подробностей. Имя дано владельцем 07.08 09:34 |
| **08.08 ①** | срочность вычисляется, а не хранится | ✅ в общем модуле контура | приёмка 5 случаев, 4 различающих, у каждого контрольная пара; прогнана на ЖИВОМ модуле. Порог 12 ч назван замером, не из головы |
| **08.08 ②** | пять полей у правила: три основания + вид и деталь условия отмены | ✅ **заполнено 30, БЕЗ основания 0 из 40 живых** | схема в базе; отказ при сохранении без основания **сработал на живом** в тот же вечер (код возврата 2, в базу не попало). ⚰️ Здесь стояло «сосуд врезан, заполнено 0 из 45» — верно на 14:40 и неверно к вечеру |
| **08.08 ③** | память роли: слой, собираемый машиной | ✅ врезано в живой инструмент | пересобирается при каждом чтении ⇒ хранить нечего, протухать нечему. **Закрыто чужой рукой:** координатор назвал, что блок ему дал |
| **08.08 ④** | адресат: таблица ИМЁН + витрина «только моё» | ✅ работает на живой ленте | 5603 строки на 1015 записок; экономия чтения 67–98 % по ролям. Разобранное машиной помечено отдельно от объявленного человеком |
| **08.08 ⑤** | постоянная проверка «обязательный довод потерян в вызове» | ✅ зелёный на живом коде | 7 случаев, 6 различающих; 76 файлов, 9 вызовов под надзором, находок 0. **Дважды краснел на исправном** — оба ложных красных пойманы до сдачи |
| **08.08 ⑥** | журнал изменений схемы | ✅ **ПОЧИНЕН 15:08 координатором** | было: база отвечала «v3, шагов 7, сверх рубежа 0» при четырёх применённых изменениях. Стало (замер 09.08 13:36): **15 шагов**, шаг пишет себя САМ в той же транзакции, у записи хранится отпечаток структуры, сторож сверяет ОБЕ стороны. ⚠️ Остаток: приёмки с нарочной поломкой у журнала НЕТ (карточка #135) |
| **09.08 ①** | выбор испытуемой копии + шаги схемы в шаблоне | ✅ построено, приёмка 14 случаев | одно место решает, что испытывать; заимствование из живого при заказанной шаблонной копии ОТКАЗЫВАЕТ, а не зеленеет. 🔴 переведена 1 приёмка из 26 (карточка #149) |
| **09.08 ②** | формы вызова из памяти ролей проверяются ЗАПУСКОМ | ✅ 33 команды, 26 отвечают, 0 падают | прежде проверялось чтением: команда могла быть «правильной на вид» и неисполнимой. Приёмка ломает разбор параметра у каждого механизма по очереди — 6 механизмов, слепых 0 |
| **09.08 🔴** | витрина «только моё» в пустой ветке | 🔴 **ЛЖЁТ, найдено чужой рукой 08.08 16:52** | печатает «Нет непрочитанных сообщений» — утверждение о ЛЕНТЕ, а не о подмножестве. Подтверждено по коду 09.08: строка общая с обычным чтением. НЕ ПОЧИНЕНО |

**Легенда.** ✅ сделано и проверено прогоном · ⏳ в работе · ⬜ не начато · ⛔ ждёт вашего слова.
«Написан и проверен» **не означает «применён к базе»**, а применён к базе — **не означает
«им пользуются»**. Третью графу я завёл после того, как перепутал это сам.

### 📈 Проверка жизнью: сосуд есть — наполняется ли?
«Схема встала» и «механизм работает» — **разные утверждения**. Второе проверено отдельно.

| механизм | 06.08, через 2 ч после наката | 07.08 13:55 | 08.08 14:40 | 08.08 23:32 | **09.08 13:36** |
|---|---|---|---|---|---|
| реестр ролей | ✅ живёт | ✅ живёт | ✅ 11 ролей | ✅ 11 | ✅ 11 |
| честная отметка о прочтении | ✅ живёт | ✅ живёт | ✅ 9 отрезков | ✅ 9 | ✅ 9 |
| критерий приёмки | ✅ живёт | ✅ живёт, стал обязательным | ✅ без критерия 14 из 132 | ✅ живёт | ✅ задач 149, открытых 85 |
| связь ответа с вопросом | ⛔ 0 строк | ✅ 86 | ✅ 125 | ✅ 126 | ✅ **126** |
| связь записки с задачей | ⛔ 0 строк | ✅ 34 | ✅ 69 | ✅ 70 | ✅ **70** |
| **адресат: ИМЕНА в базе** | — сосуда не было | — сосуда не было | ✅ 5658 | ✅ 5918 | ✅ **5942** |
| — из них объявлено ручкой | — | — | ⚠️ **55** строк, 3 роли | ✅ 315 — привычка пошла | ✅ **339** на 46 записках |
| срочность | не мерено | 🔴 гашение: 1 из 537 | ✅ вычисляется | ✅ вычисляется | ✅ вычисляется |
| **основание у правила** | — сосуда не было | — сосуда не было | 🔴 **0 из 45** | ✅ 27 из 40 | ✅ **заполнено 30 · БЕЗ основания 0 из 40** |
| **права роли полями** | — | — | — сосуда не было | ✅ 3 живых, 2 потрачены | ✅ **3 стоячих · 3 разовых, ВСЕ потрачены** |
| **разгон сна между синками** | — | — | — сосуда не было | ✅ 2 роли, считает сам | ✅ **4 роли** |
| **время в подпись** | ставила роль | ставила роль | ставила роль | ✅ ставит инструмент | ✅ ставит инструмент |
| **выбор испытуемой копии** | — | — | — | — сосуда не было | 🟡 **1 приёмка из 26** |

📌 **Строка «права роли» — самая говорящая в этой редакции.** Три разовых разрешения у PROTO
все помечены потраченными, последнее — сегодняшнее, на починку упавшей команды. Механизм
«разовое право ТРАТИТСЯ, и это видно запросом» работает на живом: сегодня я спросил базу,
что мне разрешено, и получил **одно** живое право — общее на отправку кода. Прежде такой
вопрос было некому задать, и роль отвечала на него памятью.

📌 **Строка «объявлено ручкой» — самая говорящая в таблице.** В 14:40 там стояло 55 строк
у трёх ролей, и я записал: «сосуд работает, привычка ещё нет — мерить снова через сутки».
Перемерил через девять часов: **315**. Роли начали объявлять адресата сами, без напоминаний.
⚖️ Это первый за неделю случай, когда «сосуд завели» перешло в «им пользуются» БЕЗ уговоров.
Разница с четырьмя мёртвыми механизмами одна: **этот лежит на пути, по которому и так ходят.**

---

# ЧАСТЬ 6. ЧТО СТАЛО СВОДОМ ПРАВИЛ 07.08 — задача перестала жить в чате

Ваше требование 10:20 UTC: каждая просьба, требующая работы, становится задачей с критерием
приёмки и переживает чистку контекста и перезапуск роли в новом чате. И поправка 10:22 UTC,
оказавшаяся точнее самой задачи:
> «моя задача… не самая главная. Главное — чтобы ты не забыл чем занимался до этого,
> и доделал ту работу!!»

📌 Проверил на себе: **ни одна из моих пяти задач не покрывала того, чем я занимался всё утро.**

**Внесено в свод правил** (замок владельца, версия 3). До этого в своде было 44 правила
и **ни одного про задачи** — проверено запросом, не памятью.
```
критерий приёмки ОБЯЗАТЕЛЕН ..... ни завести, ни закрыть без него   (ваше слово 12:56 UTC)
описание задачи НЕПУСТОЕ ........ заголовок описанием не считается   (ваше слово 12:56 UTC)
ссылка РАЗРЕШИМА СНАРУЖИ ........ «карточка #N» / «записка #N»,
                                   принуждение ПРЕДУПРЕЖДЕНИЕМ       (ваше слово 13:01 UTC)
файл с правилами пересобирается САМ при любом изменении правила      (ваше слово 13:40 UTC)
```

### Замер, на котором это стоит — 50 последних задач
```
ни одна из 50 не отвечает на все семь вопросов о контексте · медиана — ТРИ
нет срока годности у 47 · границы у 39 · цены ошибки у 36 · чисел у 35 · критерия у 25
неразрешимых ссылок: в задачах 33 из 50 (66 %) · в ЛЕНТЕ 166 из 200 (83 %) — втрое хуже
две задачи ЗАКРЫТЫ с пустым описанием — что сделано, не восстановимо ничем
```

### Почему обязательных полей ДВА, а не семь
> **Обязательно лишь то, что кто-то ЧИТАЕТ на следующем шаге. Поле, которое никто
> не потребляет, превращается в отписку.**
Довод проверен на себе: мой собственный признак «повод» засчитывает слова «слово владельца»
БЕЗ ДАТЫ — проверка, написанная часом раньше, уже проходится формально.

### Новые проверки — каждая со своей проверкой на нарочной поломке
```
надпись механизма расходится с механизмом ... 3 живые находки, ложных 0.
                                              Ловит 3 случая из 6 и ПЕЧАТАЕТ СВОЙ ПОТОЛОК
ссылка неразрешима снаружи .................. различает употребление и упоминание
понятна ли задача без чата, где родилась .... даёт распределение нехваток, а не приговор
файл с правилами отстал от базы ............. называет расхождение поимённо
```

---

# ЧАСТЬ 7. ЧТО МЫ ПОНЯЛИ — только то, что меняет решения

**① Механизм оживает не от уговоров, а от повода.**
06.08 я записал правило: «живут механизмы, ложащиеся на существующий жест; механизм под жест,
которого нет, уговорами не оживает». Связь записки с задачей была ровно таким случаем — и я
объявил, что уговоры не помогут. Она ожила: 0 → 34. **Не от уговоров и не сама** — сменилась
работа, задачи стали заводить на каждую просьбу, и номер задачи стало ЗАЧЕМ писать.
> **Жест не «есть» или «нет» — он появляется, когда появляется нужда.**
Правило про уговоры верно. Вывод «нет жеста ⇒ не нужен вовсе» снят как слишком сильный.

**② Проверка, вечно красная на исправном коде, опаснее отсутствующей.**
Её перестают звать через неделю — и она молчит уже по-настоящему, когда дефект вернётся.
Оплачено дважды за час: одна проверка краснела на ВЕРНОЙ правке координатора (я обвинил его
зря), другая печатала «ноль нарушений» и **не могла напечатать иного ни при каких данных**.
> **Ложный ноль опаснее ошибки: он читается как чистота и закрывает вопрос.**

**③ След обязан переживать событие, о котором свидетельствует.**
Запись о выдаче записок стиралась в момент подтверждения — то есть ровно тогда, когда
расхождение и возникает. За сутки контур нашёл четыре таких случая. Отсюда правка: не стирать,
а помечать подтверждённой.

**④ Совпадение формы принимает себя за совпадение смысла.**
Я завысил покрытие проверки с трёх случаев до четырёх, потому что в тексте была подстрока,
похожая на имя флага. Тот же класс: строка «6 ролей из 8» прочлась как утверждение о составе.

**⑤ Находка, которую не посмотрели глазами, — не находка, а гипотеза с уверенным лицом.**
Три первые находки проверки «файл отстал от базы» выглядели как недоработка координатора.
Все три оказались её собственными ошибками разбора. Ни одна не была объявлена.

**⑥ Знание, положенное в измеритель, не переезжает в отчёт само.**
У меня был инструмент, отвечающий на вопрос «работает ли механизм», и я его не позвал —
написав в доску состояния число, которого в базе нет.

**⑦ Проверка перехода не проверяет жизнь после него.**
Пометку «перенесено» я сделал значением по умолчанию — и её получила каждая новая записка.
Проверки этого не поймали: они испытывали **момент перехода**, а не **следующий день**.

---

## 📌 Закрытые вопросы — чтобы не поднимались заново

- имена ролей в публичном репозитории: решено не тратить на это время (06.08 14:33 UTC);
- история репозитория остаётся как есть (06.08 14:24 UTC);
- файл с правилами и ссылки на него **остаются**, но пересобираться должен сам (07.08 13:40 UTC);
- гашение срочности по времени отклонено владельцем — только по ответу;
- таблица правил в переносе НЕ участвовала намеренно: у неё есть колонка, которой в новой
  схеме нет. Это единственный **ломающий** шаг во всём переводе, его нельзя прятать в общий
  сценарий — понесу отдельно.

---
_PROTO · план собран 2026-08-06 12:47 UTC · дополнен предложениями соседнего контура и порядком
работ 13:22 UTC · доска состояния 17:54 UTC · Ш6 и Ш7 внесены 2026-08-07 09:55 UTC ·
переписан начисто 14:00 UTC: убраны промежуточные отчёты и жаргон, добавлен словарь,
все пункты сохранены, статусы перемерены запросом к базе._

_**Редакция 2026-08-09 13:36 UTC.** Внесены: проба-канарейка и её находки · выбор испытуемой
копии · самодостаточность образца · закрытие долга ленты · три новых долга, найденных
ЧУЖИМИ прогонами (витрина в пустой ветке · проверка форм вызова · сборщик диска).
⚰️ Четыре строки сняты НАДГРОБИЯМИ, а не молча: «сосуд пуст 0 из 45» · «журнал схемы сломан» ·
«кого берём пробой» · «тринадцать правил координатора». Каждая была верна на свой час
и спорила с числами в этом же документе — **устаревшая строка внутри собственного документа
опаснее отсутствующей: её читают как перемеренную.** Все числа этой редакции взяты запросом
к живой базе и прогоном, ни одно не перенесено из прежней редакции._
