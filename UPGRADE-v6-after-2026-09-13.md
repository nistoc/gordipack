# Обновление контура на v6: что вошло в пакет после дополнения 13.09

> Для контура на отметке **v6**, прошедшего документ [`UPGRADE-v5-to-2026-09-07.md`](UPGRADE-v5-to-2026-09-07.md)
> (разделы 3 и 7). Порядок действий общий для любого выпуска — [`UPGRADE.md`](UPGRADE.md), раздел 3;
> здесь — ЧТО нового и что с этим сделать. Охват — коммиты пакета после `ddaa4ea` (2026-09-13 12:43 UTC)
> по `c0cee1c` (2026-09-25 23:34 UTC). Источники — журнал git пакета и письма эталонного контура
> (Atlas) соседям AIA и tapas за 13–25.09. Номера карточек и записок в тексте — эталона. Время — UTC.

## 1. Коротко — что изменилось и что сделать вам

| что | зачем вам | что сделать |
|---|---|---|
| шаг схемы `20260915-foreign-rebirth-marks` | контур, применивший шаг `20260905-phoenix-history-archive` до 15.09, несёт 18 чужих отметок пересоздания ролей | прогнать шаг (раздел 2) |
| шаг схемы `20260924-role-sessions-transcript-id` | второй идентификатор чата: по нему хук узнаёт роль | прогнать шаг (раздел 2) |
| `update-tools.py`: `--rev`, `--record-source`, `--merge`/`--accept-merge`, привоз новых звеньев | обновляться до названного коммита; сводить свою правку с правкой пакета, не теряя ни одну | раздел 3.1 |
| **правила пакета приезжают ОТДЕЛЬНО** — `rules-from-pack.py` | `update-tools.py` правил не трогает вовсе | раздел 4 |
| свод пакета 53 → 56 → 67 правил, 13 приложений | уроки 13–25.09 и сокращённые тексты с разбором случаев в приложениях | `rules-from-pack.py --adopt` / `--annexes` |
| срок правил приезжает из пакета (5fb77fd) | правило с условием снятия («пока не наступит X») больше не становится бессрочным — ни в новом контуре, ни при `--adopt` | контур, собранный из пакета раньше: `rules-from-pack.py` назовёт, у каких правил срок можно взять (раздел 4.2) |
| правила ритма сверок | в НОВОМ контуре рождаются снятыми; у вас — ваше решение | раздел 4.3 |
| новые и изменённые инструменты | `remove-rows.py`, `set-rule.py --revoke/--annex`, `backup-db.py`, `save-phoenix.py --dry-run` | раздел 3 |
| холостой прогон (`--dry-run`) пишущих инструментов | работает на копии базы в памяти: до живой базы не доходит никакая запись | ничего — приезжает с `update-tools.py` |
| новые проверки общего прогона | приёмки со средой стенда, невидимые управляющие байты, ссылки на разделы памяти | ничего — приезжают с `update-tools.py` |
| Перископ: «Динамика задач» | сколько задач пришло, закрыто и осталось, по дням и ролям | ничего |

## 2. Схема — два шага после 13.09

Оба идемпотентны, оба понимают `--dry-run`; порядок — перечень `scripts/schema_step_order.py`.
Ставятся ПОСЛЕ двух шагов 13.09 (`UPGRADE-v5-to-2026-09-07.md`, раздел 7.1).

```
python <КОНТУР>/.mezosync/scripts/migrations/20260915-foreign-rebirth-marks.py --dry-run
python <КОНТУР>/.mezosync/scripts/migrations/20260915-foreign-rebirth-marks.py
python <КОНТУР>/.mezosync/scripts/migrations/20260924-role-sessions-transcript-id.py --dry-run
python <КОНТУР>/.mezosync/scripts/migrations/20260924-role-sessions-transcript-id.py
```

- **`20260915-foreign-rebirth-marks`** (коммит f817d6e, 15.09 14:38). Заводит таблицу
  `role_rebirths_foreign` и в контуре, отличном от эталона, ПЕРЕНОСИТ (не удаляет) туда отметки
  пересоздания, засеянные шагом `20260905-phoenix-history-archive` до его правки 15.09. Различитель —
  автор и источник строки (`noted_by='tool:20260905-phoenix-history-archive'`, источник
  `transcript:…`), а не имя роли: поэтому находятся и строки, чьи роли вы переименовали. Холостой
  прогон печатает, сколько и по каким ролям перенёс бы (у контура, применившего шаг до 15.09,
  таких строк 18).
  Проверка: `SELECT role, at, source, moved_at, moved_by, reason FROM role_rebirths_foreign ORDER BY role, at;`
- **`20260924-role-sessions-transcript-id`** (коммит 5c67fd9, 24.09 09:57). Добавляет необязательную
  колонку `transcript_id` в `role_sessions` и `role_sessions_history`: имя файла записи разговора, по
  которому хук узнаёт роль. Это ДРУГОЕ значение, чем `session_id` (тот доставляет сообщения). Шаг
  ничего не заполняет — значение приходит само сверкой `signal-templates.py` с хранилищем сессий
  приложения. Требует шаг `20260913-role-sessions-session-id`.

После обоих: `SELECT * FROM schema_version` → `v6 · <ваше число шагов> · 4` (четыре шага после
отметки v6: два от 13.09 и эти два). Новой отметки версии нет.

## 3. Инструменты

### 3.1 `update-tools.py` — обновление инструментов
| флаг | что делает | с коммита |
|---|---|---|
| `--rev <коммит>` | взять из источника РОВНО этот коммит, а не последний | 28f396e, 13.09 |
| `--record-source` | переписать записанный источник (`meta.template_source`); без флага разовый `--source` постоянным не становится | 28f396e, 13.09 |
| `--merge <ФАЙЛ>` | свести три текста — опору, вашу правку и пакет — в черновик `.mezosync/merge-work/`; живой файл не трогается, печатается «пересечений: N» | 8393b67, 14.09 |
| `--accept-merge <ФАЙЛ> --apply` | положить сведённый черновик; отказ, пока в нём остаются отметки пересечения | 8393b67, 14.09 |

- **Новые звенья образца** (`vnext/prototype`), которые зовут инструменты пакета, с коммита d32e621
  (14.09 22:39) приезжают сами — строкой «+» в плане. Ваш `update-tools.py` старше — прогоните
  `--apply` дважды: первый прогон привезёт новый `update-tools.py`, второй — звенья.
- ⚠️ `--merge` на коммите 8393b67 давал ложные пересечения на файлах с концами строк Windows;
  исправлено в 28a605b (14.09 15:33). Сводили на 8393b67 — сведите заново.

### 3.2 Остальные инструменты
| инструмент | что изменилось | коммит |
|---|---|---|
| `rules-from-pack.py` (новый) | сверка вашего свода правил с базой правил пакета и взятие нового — раздел 4; с 5fb77fd `--adopt` берёт и срок правила, новый флаг `--expiry-from-pack` (раздел 4.2) | 7d0899c (14.09), 7960540, 5fb77fd (25.09) |
| `set-rule.py` | `--revoke --revoked-by --reason [--revoked-at] --apply` — снять правило одним вызовом (статус, час, кто решил, причина, пометка в тексте, журнал); `--annex` — показать приложение к правилу | bbacbaa, 24.09 |
| `remove-rows.py` (новый) | снятие строк со следом в журнале изменений одной транзакцией; обязательны `--table --where --param --actor --basis`; число или строку прямо в `--where` отвергает (только `?` и `--param`); условие на всю таблицу — только с `--all-rows` | 134d9b8, fc507bd (14.09) |
| `backup-db.py` | разворот копии проверяется ВСЕГДА (`--verify` оставлен для прежних вызовов); таблицы полнотекстового поиска пересобираются после разворота; файл заменяется только после успешной проверки | 0330860 и др. (14.09) |
| `dryrun.py` (общий холостой прогон `backlog.py`, `track.py`, `write-message.py`, `save-phoenix.py`) | при `--dry-run` источник открывается только на чтение и копируется в базу в памяти — запись не доходит до живой базы НИКАКИМ способом (прежде `with conn:`, `executescript`, `COMMIT` и автофиксация обходили защиту; живых таких вызовов не было). Занятая база — отказ через 10 с словами «живая база не тронута», код не 0. Опечатка пути при `mode=ro`/`mode=rw` — тот же отказ, что у настоящего прогона | 8f71da9, 25.09 |
| `save-phoenix.py` | холостой прогон больше не пишет записи памяти в живую базу (карточка #656); отчёт сохранения — «было N · изменено K · пропало L», в список пропавших попадают только пропавшие строки | e2a639c (25.09), 0330860 (14.09) |
| `write-message.py` | адресат, до чьего чата сигнал не дойдёт (адрес не записан, сессия не найдена или в архиве), называется поимённо перед строкой «OK #» — предупреждение, не отказ | e964aa6, 24.09 |
| `signal-templates.py` | записываемый адрес сверяется с хранилищем сессий приложения: архивная или неизвестная сессия отвергается со списком живых; без хранилища — «сверить нечем» | 5c67fd9, e964aa6 (24.09) |
| `gordi-issue.py` | координатор находится по описанию роли в реестре, без буквального «COORD» и без оглядки на регистр | ea779d1 (20.09), f817d6e |
| `rules-to-skills.py` | состав навыков — из `skill-packages.json` рядом с базой либо встроенный; правило со снятым статусом навык не несёт | 0b022e3 (25.09) и ранее |
| `init-group.py` (новый контур) | сам накатывает шаги схемы по `schema_step_order.py`, останавливаясь на первом отказе; кладёт приложения правил (шаг 7е); записывает, от какой версии правил пакета контур начал жить | f817d6e, 7d0899c, 7960540 |

### 3.3 Новые проверки общего прогона (`guard-all.py`)
- **среда стенда у приёмок** (`check-acceptance-env.py`): приёмка, поднимающая временный контур, обязана
  передать ЕМУ свою среду, иначе инструменты стенда пишут в контур вызывающего (у эталона 13.09 так
  записалась строка в живую базу — починено до выпуска). Без реестра известного долга (контур из
  пакета) — предупреждение, не провал;
- **невидимые управляющие байты** в тексте (`guard-control-chars.py`, предложение AIA);
- **ссылка на раздел памяти без адресата** (`guard-memory-section-refs.py`, по мотивам AIA,
  переписано под кириллицу и регистр);
- общий прогон различает «звено не приехало с обновлением» и «звено пропало» (d32e621, 14.09).

## 4. Свод правил

### 4.1 Что в пакете
Универсальный набор по сборкам базы правил пакета: **53** (14.09) → **56** (15.09) → **67** (25.09).
- 25.09 (d61e2ca) — 11 новых ключей: `command-picks-target-by-position` · `finding-covers-only-your-branch`
  · `name-is-a-claim-not-a-property` · `refusal-text-verbatim` · `secrets-never-in-command-line` ·
  `setting-carrier-outlives-the-setting` · `direction-focus` · `insertion-registry` ·
  `code-names-in-english` · `helpers-on-simpler-models` · `history-compression-policy`;
  21 правило сокращено или поправлено, у 13 из них разбор случаев вынесен в `rules/annex/<ключ>.md`,
  а текст кончается строкой о приложении. Семь правил получили условие «если в контуре есть ритм
  сверок»; у `rhythm-survives-rebirth` безусловным стал пункт ⓪ — роль записывает свой адрес при
  каждом пробуждении.
- 15.09 (a1b8f1e) — три правила, на которые ссылались инструменты пакета, а пакет их не нёс:
  `critical-needs-basis`, `memory-hot-and-archive`, `sync-alarm-in-chat`.
- 14.09 — уроки 13.09 в стартовых правилах (в их числе `acceptance-isolated-from-live`,
  `acceptance-e2e`, `right-to-registry-same-move`, `schema-step-records-itself`).
- В новом контуре у всех правил заполнено основание «посеяно из шаблона» (прежде пусто у 44 из 56).
- 25.09 (5fb77fd) — у правил объявлен срок действия: у 61 из 67 (бессрочно 43 · до события 13 ·
  пока меряется 5, то есть с условием снятия 18). Новый контур получает его при сборке; шесть правил
  без объявленного срока получают заготовку «до пересмотра владельцем нового контура». Условия шести
  правил обезличены — без имён ролей, репозиториев и дат эталона. Тексты правил при этом не менялись.
- 25.09 (ca48498) — `owner-reply-format`, версия 3: в «Чем держится» — стиль ответа хоста первичен
  и доходит до уже открытых чатов; автоматическую проверку формы заводят только по слову владельца;
  выключение модуля в настройках хоста доходит лишь до чатов, открытых после него (уже открытый чат
  держит модуль до перезапуска). У вас сверка покажет «изменено в пакете»; правило заперто
  владельцем — `--adopt` по слову вашего владельца. Две другие правки эталона после 07:03
  (`coord-commits-coordination`, `role-roster-and-zones`) касались только прав его ролей и в пакет
  не шли.

Сколько из этого ново ИМЕННО ДЛЯ ВАС — скажет `rules-from-pack.py`: он сверяет по вашей базе, а не
по этому списку.

### 4.2 Как взять — `rules-from-pack.py`
```
python <КОНТУР>/.mezosync/scripts/rules-from-pack.py                   список расхождений по состояниям
python <КОНТУР>/.mezosync/scripts/rules-from-pack.py --show <ключ>     текст пакета, ваш текст, приложение
rules-from-pack.py --record-base --actor <роль> --apply                впервые: опора там, где текст уже совпадает
rules-from-pack.py --adopt <ключи> --word "<кто разрешил · когда · где>" --actor <роль> --apply
rules-from-pack.py --adopt <ключи> --expiry-from-pack --word "..." --actor <роль> --apply   срок пакета и поверх вашего
rules-from-pack.py --merge <ключ> --file <сведённый текст> --word "..." --actor <роль> --apply
rules-from-pack.py --skip <ключ> --word "..." --apply                  отказ, запоминается по тексту пакета
rules-from-pack.py --annexes --actor <роль> --apply                    приложения ключам с совпавшим текстом
```
`--word` обязателен при любой записи: взять чужой текст — решение вашего владельца или координатора,
инструмент подлинность слова не проверяет и говорит об этом прямо. Правило, запертое владельцем,
требует слова именно владельца. Приложение `--adopt` кладёт тем же ходом; ваше приложение с другим
текстом не переписывается без `--annex-force`. В шапке каждого приложения сказано: номера записок
и имена ролей в нём — эталона, в вашей ленте под теми же номерами лежит другое.

**Срок правила (с 5fb77fd, карточка #650).** `--adopt` кладёт вместе с текстом срок, объявленный
в посеве пакета, — когда у вас срок пуст или стоит заготовка сборки «до пересмотра владельцем нового
контура». Свой срок контура он не заменяет, а называет рядом со сроком пакета; заменить — тот же вызов
с `--expiry-from-pack`. У ключа, текст которого уже совпадает с пакетом, `--adopt` меняет только срок
и основание — текст и версия остаются те же. Список без флагов печатает внизу:
- «⏳ срок пакета можно взять у N правил» — с готовой командой `--adopt`;
- «📌 срок у вас свой у M правил» — с готовой командой `--adopt … --expiry-from-pack`.

`--summary` добавляет вторую строку «срок правил пакета: взять можно у N · у вас свой у M» — только
когда такие правила есть. Кого это касается:
- **контур, собранный из пакета до 5fb77fd:** у правил с условием снятия стоит заготовка сборки —
  они придут строкой «⏳»;
- **ключи, взятые `--adopt` до 5fb77fd как новые:** получили «бессрочно» и теперь числятся вашим
  сроком, строкой «📌». Тот же вызов `--adopt <ключи>` без `--apply` печатает у каждого ключа ваш
  срок и срок пакета; если срок пакета верен для вас — возьмите его `--expiry-from-pack`.

`--merge` срок пакета не переносит — только `--adopt`.

### 4.3 Правила ритма сверок — ваше решение
`sync-alarm-in-chat` и `sync-sleep-backoff`: в НОВОМ контуре рождаются снятыми (слово владельца
эталона 25.09 06:46:49 UTC «Новые — без будильников»). Текст в пакете прежний, и база правил пакета
статуса снятия не хранит — у вас сверка покажет «совпадает», а не «снято в пакете».
- **Снять:** `set-rule.py --key sync-alarm-in-chat --revoke --revoked-by owner --reason "…" --apply`,
  то же для `sync-sleep-backoff`.
- **Оставить:** впишите оба ключа в свой `skill-packages.json` — иначе пересобранный навык сверки
  их потеряет: сборщик образца вынул их из состава по умолчанию.

## 5. Что заметят роли
- **Будильники сверок необязательны** (e964aa6, 24.09). Пока правило `sync-alarm-in-chat` действует,
  промпт пересоздания требует файл-поручение будильника; снято — промпт собирается без него,
  со строкой «ритм: правила в своде нет — спроси владельца». Чтение ленты при снятом
  `sync-sleep-backoff` вместо времени следующей сверки печатает «с прошлого чтения: чужих записок N ·
  письма соседей в мосте: …».
- **Адрес роли — два идентификатора** (раздел 2): `session_id` доставляет сообщения,
  `transcript_id` опознаёт роль хуком; второй заполняется сам.
- **Предупреждение «адресат недостижим сигналом»** при записке — раздел 3.2.
- **Холостой прогон сохранения памяти ничего не пишет** — раздел 3.2.

## 6. Перископ (просмотр базы)
- вкладка «Динамика задач»: сколько задач появилось, закрыто и осталось — по дням и по ролям,
  фильтры периода, ролей и того, какие статусы считать закрытыми (bd0abdd, 21d0349 — 14–15.09);
- у вида «Поток» две шкалы: слева новых и закрыто, справа открыто (b4ecc99, 14.09);
- открытая вкладка живёт в адресе страницы и переживает обновление и «назад» (856a07a, 14.09);
- проверка «Перископ только читает» берёт список маршрутов из кода службы и печатает «обойдено N из M»
  (a1b8f1e, 15.09, по находке tapas).

## 7. Известно и в этом выпуске НЕ сделано
- **Срок правил доменных наборов** (`rules/domain-specific/*.sql`) не объявлен: их правила при сборке
  получают заготовку «до пересмотра владельцем нового контура». Объявлять срок там — отдельная работа.
- **Полный набор приёмок пакета** (`vnext/prototype/bite-all.py`, 185 приёмок) на свежем контуре из
  `c0cee1c` (25.09 23:35–23:55 UTC): прошли 162 · не прошли 16 · не запустились 7. Из этих 23 двадцать
  две не проходили и в `e72331f` (контрольный прогон того же дня) — давний долг набора приёмок, разбор
  отдельной работой; одна (`bite-tasks-by-track.py`) не запустилась из-за занятого файла службы
  просмотра базы на машине эталона. Новых провалов после `e72331f` нет. Прогон по копии скриптов
  в самом пакете (`--target template`) даёт больше провалов: скриптам не хватает контура вокруг.
- Статус «снято» у правил ритма до уже собранных контуров не довозится — отдельная работа
  (≈3–4 ч), владелец эталона её не выбрал.
- `role-rights.py revoke` чужого права требует роль с буквальным именем «COORD» — у координатора
  с другим именем (у AIA — `COORD-A`) это тупик. Карточка #648, ждёт слова владельца эталона.
- `bite-backup-restore.py` вне эталона не доходит до своих случаев: зовёт инструмент хранилища
  резервных копий эталона, которого в пакете нет; с 14.09 говорит об этом словами.
- Не измерено эталоном: доходит ли сигнал в ЗАКРЫТЫЙ чат и ждёт ли его открытия.

## 8. Как убедиться, что встало
```
python <КОНТУР>/.mezosync/scripts/db-q.py "SELECT * FROM schema_version"   # v6 · N · 4
python <КОНТУР>/.mezosync/scripts/update-tools.py --rev <коммит>          # «было <коммит> · стало <коммит>», изменений нет
python <КОНТУР>/.mezosync/scripts/rules-from-pack.py --summary            # новых 0 · изменено в пакете 0 (кроме осознанно отложенных); строки о сроке нет или в ней только ваш осознанный срок
python <КОНТУР>/.mezosync/scripts/guard-all.py                            # общий прогон: новых провалов нет
```
