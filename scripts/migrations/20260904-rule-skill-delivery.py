# -*- coding: utf-8 -*-
"""20260904-rule-skill-delivery — ДОСТАВКА ПРАВИЛА ДО ПОДСКАЗОК РОЛЕЙ ПОЛЕМ (карточка #526).

ПОВОД — находка COORD 2026-09-04 10:31 UTC, на своей же правке, замером по часам:
он завёл правило, ТЕМ ЖЕ ходом пересобрал подсказки ролей, проверка свежести ответила
«✅ все навыки собраны по нынешнему своду» — а правила нет НИ В ОДНОЙ подсказке.

    10:31:19  правило заведено, свод принял
    10:31:29  подсказки пересобраны: «навыков 10 · ЗАПИСАНО», отставших 0
    10:31:41  сплошной поиск по подсказкам: нового правила там НЕТ

⚡ КОРЕНЬ: состав пакетов задан РУКАМИ, списком ключей внутри сборщика. Правило,
не названное в списке, не попадает никуда — и у НЕВКЛЮЧЕНИЯ НЕТ ПРИЗНАКА: «решили
не брать» и «забыли взять» выглядят одинаково, их просто нет в списке.

🩸 И признаю авторство слепоты: сборщик и проверку писала PROTO, и слепы они в одном
месте ПО ЕЁ ЗАМЫСЛУ — «состав задан руками, это не данные, а замысел» записано в шапке
сборщика как достоинство, и этим с состава снята всякая проверка. Сильный довод закрыл
вопрос «а что ЕЩЁ под него попадает» — под него попало «забыли».

ЧТО ДЕЛАЕТ
  ① добавляет в `rules` колонку `skill_delivery` — решение о доставке правила до ролей:
        'yes'  — правило обязано быть в каком-то пакете подсказок
        'no'   — не переезжает намеренно (постоянный запрет · данные · продуктовое)
        NULL   — НЕ РЕШЕНО. Это и есть починка: молчание перестаёт быть ответом
  ② проставляет 'yes' правилам, которые УЖЕ названы в пакетах сборщика — состав берётся
     ИМПОРТОМ из самого сборщика, а не копией списка: две копии разъехались бы молча,
     и это ровно тот класс, который шаг чинит;
  ③ записывает себя в журнал схемы (та же транзакция).

⛔ ЧЕГО ШАГ НЕ ДЕЛАЕТ НАМЕРЕННО: не проставляет 'no' никому. Решение «не переезжает»
   требует замысла по каждому правилу, а замысел рукой шага — это та же слепота,
   только записанная в базу. Правила без решения остаются NULL и краснеют поимённо
   в общем прогоне, пока их не разберёт рука.

⚖️ ПОЧЕМУ ПО УМОЛЧАНИЮ «НЕ РЕШЕНО», А НЕ 'no': значение по умолчанию 'no' сделало бы
   новое правило молча недоставляемым — то есть сохранило бы сегодняшнюю беду под видом
   починки. Пустота, которая КРАСНЕЕТ, — единственная форма, при которой забывчивость
   отличима от решения.
"""
import argparse
import io
import os
import sqlite3
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

HERE = os.path.dirname(os.path.abspath(__file__))
SCRIPTS = os.path.dirname(HERE)
sys.path.insert(0, SCRIPTS)
from schema_journal import record_step, verify  # noqa: E402

VERSION = "20260904-rule-skill-delivery"


def состав_пакетов(container):
    """Ключи правил, названные в пакетах сборщика. ИМПОРТОМ, не копией.

    ⚖️ Если сборщика нет или он не читается — шаг НЕ ДОГАДЫВАЕТСЯ и не проставляет
    ничего: пустой состав молча оставил бы все правила «не решёнными», и роль увидела
    бы 64 красных вместо 27, не понимая почему. Отказ говорит, чего не хватило.
    """
    tools = os.path.join(container, 'vnext-tools')
    if tools not in sys.path:
        sys.path.insert(0, tools)
    try:
        import importlib.util
        путь = os.path.join(tools, 'rules-to-skills.py')
        if not os.path.isfile(путь):
            return None, f'сборщика нет по пути {путь}'
        spec = importlib.util.spec_from_file_location('rules_to_skills', путь)
        mod = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(mod)
        ключи = set()
        for имя, пакет in mod.ПАКЕТЫ.items():
            ключи.update(пакет['правила'])
        return ключи, None
    except Exception as e:                                   # noqa: BLE001
        return None, f'сборщик не прочитан: {type(e).__name__}: {e}'


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument('--db', default=os.path.join(os.path.dirname(SCRIPTS), 'mezosync.db'))
    ap.add_argument('--dry-run', action='store_true')
    a = ap.parse_args()

    db = os.path.abspath(a.db)
    print('=' * 78)
    print(f'{VERSION}{"  ⟨ВХОЛОСТУЮ — база не меняется⟩" if a.dry_run else ""}')
    print(f'📂 БАЗА: {db}')
    print('=' * 78)
    if not os.path.isfile(db):
        sys.exit(f'⛔ НЕ ЗАПУСТИЛСЯ: БД не найдена: {db}\n'
                 '   Путь передан --db абсолютным — корень не участвовал, проверь путь.')

    conn = sqlite3.connect(db)
    have = {r[1] for r in conn.execute("PRAGMA table_info(rules)")}
    if 'skill_delivery' in have:
        print('✅ уже сведено (колонка skill_delivery на месте) — делать нечего')
        return

    # ── состав пакетов: ИМПОРТОМ из сборщика ──
    container = os.path.dirname(os.path.dirname(SCRIPTS))
    в_пакетах, почему = состав_пакетов(container)
    if в_пакетах is None:
        sys.exit(f'⛔ НЕ ЗАПУСТИЛСЯ: состав пакетов взять неоткуда — {почему}.\n'
                 '   Шаг НЕ проставляет решения наугад: пустой состав оставил бы все\n'
                 '   правила «не решёнными» и дал бы красное, которого никто не поймёт.')

    живые = {r[0] for r in conn.execute("SELECT rule_key FROM rules WHERE status='active'")}
    все = {r[0] for r in conn.execute("SELECT rule_key FROM rules")}
    в_пакетах_живых = в_пакетах & живые
    призраки = в_пакетах - все
    снятые_в_пакетах = (в_пакетах & все) - живые
    не_решено = живые - в_пакетах

    print(f'правил в своде: {len(все)} · живых: {len(живые)}')
    print(f'названы в пакетах подсказок: {len(в_пакетах)} '
          f'(из них живых: {len(в_пакетах_живых)})')
    print(f'⇒ получат «да»: {len(в_пакетах_живых)}')
    print(f'⇒ останутся НЕ РЕШЕНО (и будут краснеть): {len(не_решено)}')
    if призраки:
        print(f'🔴 в пакете названы, а в своде их НЕТ: {sorted(призраки)}')
    if снятые_в_пакетах:
        print(f'⛔ в пакете названы СНЯТЫЕ правила: {sorted(снятые_в_пакетах)}')
    print()
    print('НЕ РЕШЕНО — поимённо, чтобы разбирали по списку, а не по числу:')
    print('─' * 78)
    for k in sorted(не_решено):
        print(f'   ⚪ {k}')
    print('─' * 78)

    if a.dry_run:
        print('\n⟨ВХОЛОСТУЮ⟩ база не тронута. Чтобы применить, прогони без --dry-run.')
        return

    conn.execute("BEGIN")
    # ⚖️ Без CHECK в ADD COLUMN намеренно: SQLite не проверяет его на уже существующих
    # строках, то есть контракт был бы объявлен и не исполнен — хуже, чем не объявлен.
    # Допустимые значения стережёт инструмент правки правил и общий прогон.
    conn.execute("ALTER TABLE rules ADD COLUMN skill_delivery TEXT")
    for k in sorted(в_пакетах_живых):
        conn.execute("UPDATE rules SET skill_delivery='yes', updated_at=updated_at "
                     "WHERE rule_key=?", (k,))
    fp = record_step(conn, VERSION,
                     "rules: доставка правила до подсказок ролей ПОЛЕМ (yes/no/НЕ РЕШЕНО). "
                     f"'yes' проставлен {len(в_пакетах_живых)} правилам, уже названным "
                     "в пакетах (состав взят импортом из сборщика, не копией); остальные "
                     f"{len(не_решено)} остаются НЕ РЕШЕНЫ и краснеют поимённо. "
                     "Карточка #526, находка COORD 04.09 10:31 UTC")
    conn.commit()
    print(f'\n✅ ВРЕЗАНО. отпечаток схемы: {fp}')
    ok, why = verify(conn)
    print(f'{"✅" if ok else "🔴"} проверка журнала: {why}')
    print('целостность:', conn.execute("PRAGMA integrity_check").fetchone()[0])
    свод = conn.execute(
        "SELECT COALESCE(skill_delivery,'не решено'), COUNT(*) FROM rules "
        "WHERE status='active' GROUP BY 1 ORDER BY 2 DESC").fetchall()
    print('контрольное чтение поля:', ' · '.join(f'{v}: {n}' for v, n in свод))


if __name__ == '__main__':
    main()
